# Skill Inventory

- Skill root: `C:\Users\jingr\codex-skills`
- LifeBoard root: `D:\LifeBoard`
- Found SKILL.md: 113
- Read successfully: 113
- Read failed: 0
- Related README/config files found: 24

## LifeBoard Stack Summary

- Vue: `^3.5.34`
- Vite: `^8.0.12`
- TypeScript: `~6.0.2`
- Pinia: `^3.0.4`
- Vue Router: `^5.1.0`
- Tailwind CSS: `^4.3.0` with `@tailwindcss/vite` `^4.3.0`
- Weather module files: 69
- Weather atmosphere asset files: 12
- Current weather visual approach: Vue SFC + TypeScript + Pinia + CSS/Tailwind tokens + local AVIF/WebP atmosphere assets. package.json has no GSAP, Three.js, or motion library dependency.

## Full SKILL.md Inventory
| Skill | Relative path | Size | SHA-256 | Categories | Priority | Read |
|---|---|---|---|---|---|---|
| 12-principles-of-animation | 12-principles-of-animation/SKILL.md | 5293 | 22b404b64202279f... | B_weather_visual, C_conditional | P1 | True |
| adapt | adapt/SKILL.md | 592 | eebd1d2d7e961366... | B_weather_visual, C_conditional | P1 | True |
| agent-browser | agent-browser/SKILL.md | 3318 | ed4735913554955e... | B_weather_visual, C_conditional | P1 | True |
| animate | animate/SKILL.md | 581 | 4e0e5f3bc64b531f... | B_weather_visual | P1 | True |
| antfu | antfu/SKILL.md | 4840 | 35bbb47ca2421290... | A_LifeBoard_core, C_conditional | P1 | True |
| audit-and-fix | audit-and-fix/SKILL.md | 851 | c4966621758c6ec3... | B_weather_visual, C_conditional | P1 | True |
| audit | audit/SKILL.md | 595 | 1ed43b2848b7207c... | B_weather_visual, C_conditional | P1 | True |
| baseline-ui | baseline-ui/SKILL.md | 3616 | bbd26ea28b6aece9... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P0 | True |
| bencium-innovative-ux-designer | bencium-innovative-ux-designer/SKILL.md | 28887 | f6e19982a2e88502... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| bolder | bolder/SKILL.md | 575 | 327aec77ad6b3836... | B_weather_visual | P1 | True |
| brandkit | brandkit/SKILL.md | 16790 | a250c7b41aecacc4... | B_weather_visual, C_conditional | P1 | True |
| budge | budge/SKILL.md | 9637 | c8be728a12a4559e... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| canvas-design | canvas-design/SKILL.md | 12068 | 7bebc4a2cf108c55... | B_weather_visual, C_conditional | P1 | True |
| clarify | clarify/SKILL.md | 580 | 2ecd41fb6aba4f98... | D_currently_unrelated | P3 | True |
| colorize | colorize/SKILL.md | 582 | 93e52eeaccf0bf83... | B_weather_visual | P1 | True |
| contrast-checker | contrast-checker/SKILL.md | 825 | 3c3bb97fdca44fe5... | B_weather_visual, C_conditional | P1 | True |
| create-adaptable-composable | create-adaptable-composable/SKILL.md | 2748 | dff8922fc0b079bb... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| critique | critique/SKILL.md | 593 | bbb51e042629d5e8... | C_conditional | P2 | True |
| delight | delight/SKILL.md | 588 | 384eff6304fd8bf2... | B_weather_visual | P1 | True |
| design-lab | design-lab/SKILL.md | 31102 | 7b18e2c3a6086d6d... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| design-taste-frontend-v1 | design-taste-frontend-v1/SKILL.md | 21421 | fb6f7658c0e4a939... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| design-taste-frontend | design-taste-frontend/SKILL.md | 88459 | 2e064e92aca020b2... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| distill | distill/SKILL.md | 582 | 4ec742327d73376c... | B_weather_visual | P1 | True |
| emil-design-eng | emil-design-eng/SKILL.md | 28014 | aa528a78ab11f91d... | B_weather_visual, C_conditional | P1 | True |
| fixing-accessibility | fixing-accessibility/SKILL.md | 4853 | 2c2a4623f333920b... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| fixing-metadata | fixing-metadata/SKILL.md | 4551 | 498a1ae121c010df... | C_conditional | P2 | True |
| fixing-motion-performance | fixing-motion-performance/SKILL.md | 5716 | bc5af058e1f6479c... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| frontend-design | frontend-design/SKILL.md | 8315 | 35c43b9d10c2388d... | B_weather_visual, C_conditional | P1 | True |
| design-taste-frontend | frontend-skills/design-taste-frontend/SKILL.md | 88459 | 2e064e92aca020b2... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| impeccable | frontend-skills/impeccable/SKILL.md | 22275 | 881f679f4c96b4cb... | A_LifeBoard_core, B_weather_visual, C_conditional | P0 | True |
| frontend-slides | frontend-slides/plugins/frontend-slides/skills/frontend-slides/SKILL.md | 28567 | 491b333f92e31f97... | B_weather_visual, C_conditional | P1 | True |
| frontend-slides | frontend-slides/SKILL.md | 28567 | 491b333f92e31f97... | B_weather_visual, C_conditional | P1 | True |
| frontend-ui-engineering | frontend-ui-engineering/SKILL.md | 10967 | 02839d25ff52f87c... | A_LifeBoard_core, B_weather_visual, C_conditional | P0 | True |
| full-output-enforcement | full-output-enforcement/SKILL.md | 2641 | 7b2275b591af7760... | C_conditional | P2 | True |
| generating-sounds-with-ai | generating-sounds-with-ai/SKILL.md | 6393 | 88ec9cf25a07e504... | C_conditional | P2 | True |
| gpt-taste | gpt-taste/SKILL.md | 7931 | 6a19b70e6b53761d... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| harden | harden/SKILL.md | 594 | eb737cb3a5d82637... | D_currently_unrelated | P3 | True |
| high-end-visual-design | high-end-visual-design/SKILL.md | 10659 | 2d11cdcefc8bf319... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| image-to-code | image-to-code/SKILL.md | 37670 | 2944189b448e2a77... | B_weather_visual, C_conditional | P1 | True |
| imagegen-frontend-mobile | imagegen-frontend-mobile/SKILL.md | 41791 | 3cadf1bfbe836d37... | B_weather_visual, C_conditional | P1 | True |
| imagegen-frontend-web | imagegen-frontend-web/SKILL.md | 37841 | 23e6d27ab410e97f... | B_weather_visual, C_conditional | P1 | True |
| impeccable | impeccable/SKILL.md | 20611 | cd90f6b3e7d2e08e... | A_LifeBoard_core, B_weather_visual, C_conditional | P0 | True |
| industrial-brutalist-ui | industrial-brutalist-ui/SKILL.md | 8548 | bd9c7fd04ae14953... | B_weather_visual, C_conditional | P1 | True |
| interaction-design | interaction-design/SKILL.md | 7892 | 05c71e38aad5026a... | B_weather_visual, C_conditional | P1 | True |
| interface-design | interface-design/SKILL.md | 23254 | 832596a89575683c... | B_weather_visual, C_conditional | P1 | True |
| layout | layout/SKILL.md | 580 | 18b7b55d4a7bac2f... | D_currently_unrelated | P3 | True |
| link-purpose | link-purpose/SKILL.md | 809 | 7dde49b0bb78d30b... | B_weather_visual, C_conditional | P1 | True |
| make-interfaces-feel-better | make-interfaces-feel-better/SKILL.md | 7951 | d180fda81e7996d0... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| mastering-animate-presence | mastering-animate-presence/SKILL.md | 7391 | 47c84356e9d71d80... | B_weather_visual, C_conditional | P1 | True |
| minimalist-ui | minimalist-ui/SKILL.md | 7986 | 1a292d1459292649... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| morphing-icons | morphing-icons/SKILL.md | 7976 | 6d9e9c9675e6d5b8... | B_weather_visual, C_conditional | P1 | True |
| next-best-practices | next-best-practices/SKILL.md | 4157 | b090e6c676678306... | C_conditional, D_currently_unrelated | P2 | True |
| next-cache-components | next-cache-components/SKILL.md | 9773 | 766c6f603dc9240d... | D_currently_unrelated | P3 | True |
| next-upgrade | next-upgrade/SKILL.md | 2059 | 1fd634ed86a62744... | C_conditional, D_currently_unrelated | P2 | True |
| nuxt | nuxt/SKILL.md | 3571 | a07f3f1ec7798935... | C_conditional, D_currently_unrelated | P2 | True |
| oklch-skill | oklch-skill/SKILL.md | 4906 | 10d1a44a0eb3669b... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| optimize | optimize/SKILL.md | 592 | 41070a97728bbe08... | B_weather_visual, C_conditional | P1 | True |
| overdrive | overdrive/SKILL.md | 603 | dcedacb22ffa82a0... | B_weather_visual | P1 | True |
| pinia | pinia/SKILL.md | 2778 | dfb6e1cca9573e65... | A_LifeBoard_core, C_conditional | P0 | True |
| playwright-cli | playwright-cli/SKILL.md | 12273 | 037cec52b1b0bc15... | B_weather_visual, C_conditional | P1 | True |
| pnpm | pnpm/SKILL.md | 2999 | cc9accac4450b048... | A_LifeBoard_core, C_conditional | P1 | True |
| polish | polish/SKILL.md | 567 | 6de4d5ebc9b48d13... | C_conditional | P2 | True |
| pseudo-elements | pseudo-elements/SKILL.md | 6840 | d934acb274fc765e... | B_weather_visual, C_conditional | P1 | True |
| quieter | quieter/SKILL.md | 571 | 148288c729856431... | C_conditional | P2 | True |
| rams | rams/SKILL.md | 1243 | c082b27a78aee986... | B_weather_visual, C_conditional | P1 | True |
| react-doctor | react-doctor/SKILL.md | 2974 | 0e2751f14fb69d4d... | C_conditional, D_currently_unrelated | P2 | True |
| react-native-best-practices | react-native-best-practices/SKILL.md | 12265 | 6f28f0d8aa36c4d6... | C_conditional, D_currently_unrelated | P2 | True |
| react-router-framework-mode | react-router-framework-mode/SKILL.md | 4711 | e75d984f18b736a1... | D_currently_unrelated | P3 | True |
| redesign-existing-projects | redesign-existing-projects/SKILL.md | 15238 | d5a62efaf36fa52e... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| refactor | refactor/SKILL.md | 793 | 9ca2d61126c23fca... | B_weather_visual, C_conditional | P1 | True |
| remotion-best-practices | remotion-best-practices/SKILL.md | 10016 | 2461d2bcaf838175... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| shadcn | shadcn/SKILL.md | 18846 | ba95c02a6f2a05b7... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| shape | shape/SKILL.md | 585 | c706f19cb7907353... | D_currently_unrelated | P3 | True |
| slidev | slidev/SKILL.md | 8971 | 961406834e793ae6... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| sounds-on-the-web | sounds-on-the-web/SKILL.md | 7837 | e145838a1e90706c... | B_weather_visual, C_conditional | P1 | True |
| stitch-design-taste | stitch-design-taste/SKILL.md | 12035 | 56e41363d61ff565... | B_weather_visual, C_conditional | P1 | True |
| svelte-code-writer | svelte-code-writer/SKILL.md | 1982 | cc8fa3ac4a382aeb... | C_conditional, D_currently_unrelated | P2 | True |
| swiftui-ui-patterns | swiftui-ui-patterns/SKILL.md | 7535 | 4e09e4c1c98f9b45... | C_conditional, D_currently_unrelated | P2 | True |
| swiss-design | swiss-design/SKILL.md | 13281 | 4b2bd543abd97ddb... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| threejs-animation | threejs-animation/SKILL.md | 13109 | d5dbcda9d08614d6... | B_weather_visual, C_conditional | P1 | True |
| threejs-fundamentals | threejs-fundamentals/SKILL.md | 12008 | c89027e35cf56629... | B_weather_visual, C_conditional | P1 | True |
| threejs-geometry | threejs-geometry/SKILL.md | 14377 | 21040e92119411a5... | B_weather_visual, C_conditional | P1 | True |
| threejs-interaction | threejs-interaction/SKILL.md | 16978 | 6a2251ce5dc088ad... | B_weather_visual, C_conditional | P1 | True |
| threejs-lighting | threejs-lighting/SKILL.md | 12508 | 33bb2d1fa9b30450... | B_weather_visual, C_conditional | P1 | True |
| threejs-loaders | threejs-loaders/SKILL.md | 14655 | deef71b4f581463e... | B_weather_visual, C_conditional | P1 | True |
| threejs-materials | threejs-materials/SKILL.md | 13938 | c6dcfb1f45e08fcf... | B_weather_visual, C_conditional | P1 | True |
| threejs-postprocessing | threejs-postprocessing/SKILL.md | 15029 | e29494e50776ed0a... | B_weather_visual, C_conditional | P1 | True |
| threejs-shaders | threejs-shaders/SKILL.md | 14698 | e9b1560195a856c8... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| threejs-textures | threejs-textures/SKILL.md | 14271 | 1708569adf18bb60... | B_weather_visual, C_conditional | P1 | True |
| to-spring-or-not-to-spring | to-spring-or-not-to-spring/SKILL.md | 7836 | 6644af74e134ad54... | B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| transitions-dev | transitions-dev/SKILL.md | 11615 | 1c76817d92f8ec0e... | B_weather_visual, C_conditional | P1 | True |
| tsdown | tsdown/SKILL.md | 12272 | 3b1d9d70c362468e... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| turborepo | turborepo/SKILL.md | 28083 | 42eec5450721c99c... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| typeset | typeset/SKILL.md | 587 | dce7c69d8433297c... | D_currently_unrelated | P3 | True |
| ui-ux-pro-max | ui-ux-pro-max/SKILL.md | 45434 | 4d89e171a3edc393... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| unocss | unocss/SKILL.md | 4299 | ff6f9b32ca1f52fd... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| use-of-color | use-of-color/SKILL.md | 809 | b5a4ed620a55ada0... | B_weather_visual, C_conditional | P1 | True |
| vercel-react-best-practices | vercel-react-best-practices/SKILL.md | 7400 | 3286ace648b4d740... | C_conditional, D_currently_unrelated | P2 | True |
| vite | vite/SKILL.md | 2535 | f9352d9b6925bed7... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P0 | True |
| vitepress | vitepress/SKILL.md | 3244 | 0b3e1e57a66250b1... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| vitest | vitest/SKILL.md | 3360 | 3dcdc45fba7d7665... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| vue-best-practices | vue-best-practices/SKILL.md | 8957 | d208d79fbc8f1ccc... | A_LifeBoard_core, B_weather_visual, C_conditional | P0 | True |
| vue-debug-guides | vue-debug-guides/SKILL.md | 21060 | 17e6900054dc24ae... | A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated | P1 | True |
| vue-jsx-best-practices | vue-jsx-best-practices/SKILL.md | 405 | d86ef3bf2b619a05... | C_conditional, D_currently_unrelated | P2 | True |
| vue-options-api-best-practices | vue-options-api-best-practices/SKILL.md | 1870 | d4a8132d9dbe9dd0... | A_LifeBoard_core, C_conditional | P1 | True |
| vue-pinia-best-practices | vue-pinia-best-practices/SKILL.md | 1191 | bb5e33ec826389ca... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P0 | True |
| vue-router-best-practices | vue-router-best-practices/SKILL.md | 1515 | 2131e4f3c487b0d0... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P0 | True |
| vue-testing-best-practices | vue-testing-best-practices/SKILL.md | 2158 | b536864c07c1b405... | A_LifeBoard_core, C_conditional | P1 | True |
| vue | vue/SKILL.md | 2532 | 670fa4c35af47acb... | A_LifeBoard_core, C_conditional, D_currently_unrelated | P1 | True |
| vueuse-functions | vueuse-functions/SKILL.md | 37572 | 1346da238bbabcc3... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| wcag-audit-patterns | wcag-audit-patterns/SKILL.md | 2591 | 71a4b3bb3d254c00... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |
| web-design-guidelines | web-design-guidelines/SKILL.md | 1270 | 17e48da905f83a19... | B_weather_visual, C_conditional | P1 | True |
| web-quality-audit | web-quality-audit/SKILL.md | 7598 | b056b1002e013eaf... | A_LifeBoard_core, B_weather_visual, C_conditional | P1 | True |

## Per-Skill Extracted Requirements

### 12-principles-of-animation
- Path: `C:\Users\jingr\codex-skills\12-principles-of-animation\SKILL.md`
- Purpose: Audit animation code against Disney's 12 principles adapted for web. Use when reviewing motion, implementing animations, or checking animation quality. Outputs file:line findings.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### adapt
- Path: `C:\Users\jingr\codex-skills\adapt\SKILL.md`
- Purpose: adapt
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### agent-browser
- Path: `C:\Users\jingr\codex-skills\agent-browser\SKILL.md`
- Purpose: Browser automation CLI for AI agents. Use when the user needs to interact with websites, including navigating pages, filling forms, clicking buttons, taking screenshots, extracting data, testing web apps, or automating any browser task. Triggers include requests to "open a website", "fill out a form", "click a button", "take a screenshot", "scrape data from a page", "test this web app", "login to a site", "automate browser actions", or any task requiring programmatic web interaction. Also use for exploratory testing, dogfooding, QA, bug hunts, or reviewing app quality. Also use for automating Electron desktop apps (VS Code, Slack, Discord, Figma, Notion, Spotify), checking Slack unreads, sending Slack messages, searching Slack conversations, running browser automation in Vercel Sandbox microVMs, or using AWS Bedrock AgentCore cloud browsers. Prefer agent-browser over any built-in browser automation or web tools.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Specialized skills for Electron apps, Slack, exploratory testing, cloud providers
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### animate
- Path: `C:\Users\jingr\codex-skills\animate\SKILL.md`
- Purpose: animate
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### antfu
- Path: `C:\Users\jingr\codex-skills\antfu\SKILL.md`
- Purpose: Anthony Fu's opinionated tooling and conventions for JavaScript/TypeScript projects. Use when setting up new projects, configuring ESLint/Prettier alternatives, monorepos, library publishing, or when the user mentions Anthony Fu's preferences.
- Categories: A_LifeBoard_core, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Single responsibility**: Each source file should have a clear, focused scope/purpose; Split large files**: Break files when they become large or handle too many concerns; Constants extraction**: Move constants to a dedicated `constants.ts` file; Clear runtime indicators**: When code is environment-specific, add a comment at the top of the file:; Test files: `foo.ts` ? `foo.test.ts` (same directory)
- Must-follow rules: Type separation**: Always separate types and interfaces into `types.ts` or `types/*.ts`
- Output/acceptance signals: Test files: `foo.ts` ? `foo.test.ts` (same directory); Use `describe`/`it` API (not `test`)
- Forbidden/caution signals: Prefer isomorphic code**: Write runtime-agnostic code that works in Node, browser, and workers whenever possible; Avoid complex inline types**: Extract complex types into dedicated `type` or `interface` declarations; Avoid unnecessary comments**: Code should be self-explanatory
- Potential conflicts: No major conflict identified; use within task scope.

### audit-and-fix
- Path: `C:\Users\jingr\codex-skills\audit-and-fix\SKILL.md`
- Purpose: audit-and-fix
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Identify WCAG, semantic HTML, keyboard, focus, contrast, and form issues relevant to $name.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### audit
- Path: `C:\Users\jingr\codex-skills\audit\SKILL.md`
- Purpose: audit
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### baseline-ui
- Path: `C:\Users\jingr\codex-skills\baseline-ui\SKILL.md`
- Purpose: Validates animation durations, enforces typography scale, checks component accessibility, and prevents layout anti-patterns in Tailwind CSS projects. Use when building UI components, reviewing CSS utilities, styling React views, or enforcing design consistency.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: `/baseline-ui <file>`; MUST use Tailwind CSS defaults unless custom values already exist or are explicitly requested; SHOULD use existing theme or Tailwind CSS color tokens before introducing new ones
- Must-follow rules: MUST use Tailwind CSS defaults unless custom values already exist or are explicitly requested; MUST use `motion/react` (formerly `framer-motion`) when JavaScript animation is required; MUST use `cn` utility (`clsx` + `tailwind-merge`) for class logic; MUST use accessible component primitives for anything with keyboard or focus behavior (`Base UI`, `React Aria`, `Radix`); MUST use the project?s existing component primitives first; MUST add an `aria-label` to icon-only buttons
- Output/acceptance signals: NEVER rebuild keyboard or focus behavior by hand unless explicitly requested
- Forbidden/caution signals: NEVER mix primitive systems within the same interaction surface; NEVER rebuild keyboard or focus behavior by hand unless explicitly requested; NEVER use `h-screen`, use `h-dvh`; NEVER block paste in `input` or `textarea` elements; NEVER add animation unless it is explicitly requested
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### bencium-innovative-ux-designer
- Path: `C:\Users\jingr\codex-skills\bencium-innovative-ux-designer\SKILL.md`
- Purpose: Create distinctive, production-grade frontend interfaces with high design quality. Use this skill when the user asks to build web components, pages, or applications. Generates creative, polished code that avoids generic AI aesthetics.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Body Text**: Prioritize legibility, reading comfort, accessibility; Spacing & Readability:**; Line length**: 45-75 characters optimal for readability (60-70 ideal); Validate inputs inline (before submission); Soft deletes (trash/archive before permanent delete)
- Must-follow rules: Every element must justify its existence; Every color must serve a purpose (hierarchy, function, status, or action); Always add loading states, spinners, placeholder animations
- Output/acceptance signals: Brutalist/raw** - exposed structure, harsh contrasts, intentionally rough; Typography combinations that create contrast; Create hierarchy through typography scale, color contrast, and spatial relationships; Accents are saturated enough to create clear contrast; Backgrounds (lightest)
- Forbidden/caution signals: NEVER Use These AI-Generated Aesthetics:**; Avoid: glass morphism, Apple mimicry (but shadows/gradients are tools, not enemies); NEVER default to Inter, Roboto, or Space Grotesk - find unique fonts; Avoid decorative colors that don't communicate meaning; NEVER use default SaaS blue (#3B82F6) or purple gradients on white
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### bolder
- Path: `C:\Users\jingr\codex-skills\bolder\SKILL.md`
- Purpose: bolder
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### brandkit
- Path: `C:\Users\jingr\codex-skills\brandkit\SKILL.md`
- Purpose: Premium brand-kit image generation skill for creating high-end brand-guidelines boards, logo systems, identity decks, and visual-world presentations. Trained for minimalist, cinematic, editorial, dark-tech, luxury, cultural, security, gaming, developer-tool, and consumer-app brand systems. Optimized for intentional logo concepting, refined composition, sparse typography, strong symbolic meaning, premium mockups, art-directed imagery, and flexible grid layouts.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: presentation-ready; unreadable labels
- Must-follow rules: accents must repeat across panels
- Output/acceptance signals: build ? frame, scaffold, block, cursor; bolt + build speed; path, leaf, moon, horizon, compass, portal, folded mark; "What will you build today?"; "Build better."
- Forbidden/caution signals: what the brand should avoid
- Potential conflicts: No major conflict identified; use within task scope.

### budge
- Path: `C:\Users\jingr\codex-skills\budge\SKILL.md`
- Purpose: Use when making single-property CSS or Tailwind visual changes in Next.js App Router projects. Presents a floating control widget on the page so the user can tweak the value before it is persisted. Covers atomic style changes, live preview, and cleanup. Triggers on single CSS value or single Tailwind utility class modifications.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Related slides** (index 1+): pick 2?3 related properties from the same element. Read their current computed/declared values. Use sensible min/max ranges.
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:** `padding: 8px` ? `padding: 16px` | `className="p-2 m-4"` ? `className="p-4 m-4"`; Your turn is done.** Do NOT open a browser, verify the panel, or wait for the user. Next.js HMR delivers the change.
- Forbidden/caution signals: Proactive skill. Next.js App Router only.** If a task results in a single CSS property or Tailwind class change, follow this skill automatically. The user should never need to say "use budge."; Your turn is done.** Do NOT open a browser, verify the panel, or wait for the user. Next.js HMR delivers the change.
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### canvas-design
- Path: `C:\Users\jingr\codex-skills\canvas-design\SKILL.md`
- Purpose: Create beautiful visual art in .png and .pdf documents using design philosophy. You should use this skill when the user asks to create a poster, piece of art, design, or other static piece. Create original visual designs, never copying existing artists' work to avoid copyright violations.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Design Philosophy Creation (.md file); Express by creating it on a canvas (.pdf file or .png file); The design philosophy should be 4-6 paragraphs long.** Fill it with poetic design philosophy that brings together the core vision. Avoid repeating the same points. Keep the design philosophy generic without mentioning the intention of the art, as if it can be used wherever. Output the design philosophy as a .md file.; CRITICAL STEP**: Before creating the canvas, identify the subtle conceptual thread from the original request.; IMPORTANT**: The user ALREADY said "It isn't perfect enough. It must be pristine, a masterpiece if craftsmanship, as if it were about to be displayed in a museum."
- Must-follow rules: Emphasize craftsmanship REPEATEDLY**: The philosophy MUST stress multiple times that the final work should appear as though it took countless hours to create, was labored over with care, and comes from someone at the absolute top of their field. This framing is essential - repeat phrases like "meticulously crafted," "the product of deep expertise," "painstaking attention," "master-level execution."; MINIMAL TEXT**: Always emphasize that text is sparse, essential-only, integrated as visual element - never lengthy; EXPERT CRAFTSMANSHIP**: Repeatedly emphasize the final work must look meticulously crafted, labored over with care, the product of countless hours by someone at the top of their field; Text as a contextual element**: Text is always minimal and visual-first, but let context guide whether that means whisper-quiet labels or bold typographic gestures. A punk venue poster might have larger, more aggressive type than a minimalist ceramics studio identity. Most of the time, font should be thin. All use of fonts must be design-forward and prioritize visual communication. Regardless of text scale, nothing falls off the page and nothing overlaps. Every element must be contained within the canvas boundaries with proper margins. Check carefully that all text, graphics, and visual elements have breathing room and clear separation. This is non-negotiable for professional execution. **IMPORTANT: Use different fonts if writing text. Search the `./canvas-fonts` directory. Regardless of approach, sophistication is non-negotiable.**; IMPORTANT**: The user ALREADY said "It isn't perfect enough. It must be pristine, a masterpiece if craftsmanship, as if it were about to be displayed in a museum."
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Avoid redundancy**: Each design aspect should be mentioned once. Avoid repeating points about color theory, spatial relationships, or typographic principles unless adding new depth.; MINIMAL TEXT**: Always emphasize that text is sparse, essential-only, integrated as visual element - never lengthy; The design philosophy should be 4-6 paragraphs long.** Fill it with poetic design philosophy that brings together the core vision. Avoid repeating the same points. Keep the design philosophy generic without mentioning the intention of the art, as if it can be used wherever. Output the design philosophy as a .md file.; IMPORTANT**: For any type of content, even if the user requests something for a movie/game/book, the approach should still be sophisticated. Never lose sight of the idea that this should be art, not something that's cartoony or amateur.; CRITICAL**: To refine the work, avoid adding more graphics; instead refine what has been created and make it extremely crisp, respecting the design philosophy and the principles of minimalism entirely. Rather than adding a fun filter or refactoring a font, consider how to make the existing composition more cohesive with the art. If the instinct is to call a new function or draw a new shape, STOP and instead ask: "How can I make what's already here more of a piece of art?"
- Potential conflicts: No major conflict identified; use within task scope.

### clarify
- Path: `C:\Users\jingr\codex-skills\clarify\SKILL.md`
- Purpose: clarify
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### colorize
- Path: `C:\Users\jingr\codex-skills\colorize\SKILL.md`
- Purpose: colorize
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### contrast-checker
- Path: `C:\Users\jingr\codex-skills\contrast-checker\SKILL.md`
- Purpose: contrast-checker
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Identify WCAG, semantic HTML, keyboard, focus, contrast, and form issues relevant to $name.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### create-adaptable-composable
- Path: `C:\Users\jingr\codex-skills\create-adaptable-composable\SKILL.md`
- Purpose: Create a library-grade Vue composable that accepts maybe-reactive inputs (MaybeRef / MaybeRefOrGetter) so callers can pass a plain value, ref, or getter. Normalize inputs with toValue()/toRef() inside reactive effects (watch/watchEffect) to keep behavior predictable and reactive. Use this skill when user asks for creating adaptable or reusable composables.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Read-only, computed-friendly input: use `MaybeRefOrGetter`
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Parameter might be a function value (callback/predicate/comparator): do not use `MaybeRefOrGetter`, or you may accidentally invoke it as a getter.
- Potential conflicts: No major conflict identified; use within task scope.

### critique
- Path: `C:\Users\jingr\codex-skills\critique\SKILL.md`
- Purpose: critique
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### delight
- Path: `C:\Users\jingr\codex-skills\delight\SKILL.md`
- Purpose: delight
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### design-lab
- Path: `C:\Users\jingr\codex-skills\design-lab\SKILL.md`
- Purpose: Conduct design interviews, generate five distinct UI variations in a temporary design lab, collect feedback, and produce implementation plans. Use when the user wants to explore UI design options, redesign existing components, or create new UI with multiple approaches to compare.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: All temporary files MUST be deleted when the process ends, whether by:**; `.css` or `.module.css` files ? **CSS Modules**; If Tailwind detected**, read `tailwind.config.js` or `tailwind.config.ts`:; If CSS Variables exist**, read `globals.css`, `variables.css`, or `:root` definitions:; If UI library detected** (MUI, Chakra, Ant), read the theme configuration:
- Must-follow rules: All temporary files MUST be deleted when the process ends, whether by:**; Always scan existing components** to understand patterns:; Question: "Is dark mode required?"; "Yes" - Must support dark mode; "Nice to have" - Support if easy, not required; "Both equally" - Must work well on all devices
- Output/acceptance signals: "Use existing components" - Build on current design system; Pass `targetName` prop with the component/page name; Color contrast: 4.5:1 for text, 3:1 for UI elements; Button** (`[data-testid='submit']`, button with "Submit"); Primary: The CSS selector in backticks (e.g., `[data-testid='submit']`)
- Forbidden/caution signals: Never leave `.claude-design/` or `__design_lab` routes behind.** If the user says "cancel", "abort", "stop", or "nevermind" at any point, confirm and then delete all temporary artifacts.; DO NOT use generic/predefined styles. Extract visual language from the project:**; Question: "What are the top pain points with the current design (or what should this new design avoid)?"; The FeedbackOverlay is the PRIMARY feature of the Design Lab.** Without it, users cannot provide interactive feedback. NEVER generate a Design Lab without the FeedbackOverlay.; Reliability Strategy:** To avoid import path issues across different project configurations, create the FeedbackOverlay **directly in the route directory** (e.g., `app/design-lab/FeedbackOverlay.tsx`), NOT in `.claude-design/`. This ensures a simple relative import (`./FeedbackOverlay`) always works.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### design-taste-frontend-v1
- Path: `C:\Users\jingr\codex-skills\design-taste-frontend-v1\SKILL.md`
- Purpose: The original v1 taste-skill, preserved for projects depending on its exact behavior. The current default is `design-taste-frontend` (v2 experimental), which is a substantial rewrite. Use this v1 install name only if you need exact backward compatibility.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: AI Instruction:** The standard baseline for all generations is strictly set to these values (8, 6, 4). Do not ask the user to edit this file. Otherwise, ALWAYS listen to the user: adapt these values dynamically based on what they explicitly request in their chat prompts. Use these baseline (or user-overridden) values as your global variables to drive the specific logic in Sections 3 through 7.; DEPENDENCY VERIFICATION [MANDATORY]:** Before importing ANY 3rd party library (e.g. `framer-motion`, `lucide-react`, `zustand`), you MUST check `package.json`. If the package is missing, you MUST output the installation command (e.g. `npm install package-name`) before providing the code. **Never** assume a library exists.; Production-Ready Cleanliness:** Code must be extremely clean, visually striking, memorable, and meticulously refined in every detail.; Surfaces:** Use `rounded-[2.5rem]` for all major containers. Apply a "diffusion shadow" (a very light, wide-spreading shadow, e.g., `shadow-[0_20px_40px_-15px_rgba(0,0,0,0.05)]`) to create depth without clutter.
- Must-follow rules: AI Instruction:** The standard baseline for all generations is strictly set to these values (8, 6, 4). Do not ask the user to edit this file. Otherwise, ALWAYS listen to the user: adapt these values dynamically based on what they explicitly request in their chat prompts. Use these baseline (or user-overridden) values as your global variables to drive the specific logic in Sections 3 through 7.; DEPENDENCY VERIFICATION [MANDATORY]:** Before importing ANY 3rd party library (e.g. `framer-motion`, `lucide-react`, `zustand`), you MUST check `package.json`. If the package is missing, you MUST output the installation command (e.g. `npm install package-name`) before providing the code. **Never** assume a library exists.; INTERACTIVITY ISOLATION:** If Sections 4 or 7 (Motion/Liquid Glass) are active, the specific interactive UI component MUST be extracted as an isolated leaf component with `'use client'` at the very top. Server Components must exclusively render static layouts.; Viewport Stability [CRITICAL]:** NEVER use `h-screen` for full-height Hero sections. ALWAYS use `min-h-[100dvh]` to prevent catastrophic layout jumping on mobile browsers (iOS Safari).; Grid over Flex-Math:** NEVER use complex flexbox percentage math (`w-[calc(33%-1rem)]`). ALWAYS use CSS Grid (`grid grid-cols-1 md:grid-cols-3 gap-6`) for reliable structures.; Icons:** You MUST use exactly `@phosphor-icons/react` or `@radix-ui/react-icons` as the import paths (check installed version). Standardize `strokeWidth` globally (e.g., exclusively use `1.5` or `2.0`).
- Output/acceptance signals: THE LILA BAN:** The "AI Purple/Blue" aesthetic is strictly BANNED. No purple button glows, no neon gradients. Use absolute neutral bases (Zinc/Slate) with high-contrast, singular accents (e.g. Emerald, Electric Blue, or Deep Rose).; Staggered Orchestration:** Do not mount lists or grids instantly. Use `staggerChildren` (Framer) or CSS cascade (`animation-delay: calc(var(--index) * 100ms)`) to create sequential waterfall reveals. **CRITICAL:** For `staggerChildren`, the Parent (`variants`) and Children MUST reside in the identical Client Component tree. If data is fetched asynchronously, pass the data as props into a centralized Parent Motion wrapper.; Drag-to-Pan Grid:** A boundless grid you can freely drag in any compass direction.
- Forbidden/caution signals: AI Instruction:** The standard baseline for all generations is strictly set to these values (8, 6, 4). Do not ask the user to edit this file. Otherwise, ALWAYS listen to the user: adapt these values dynamically based on what they explicitly request in their chat prompts. Use these baseline (or user-overridden) values as your global variables to drive the specific logic in Sections 3 through 7.; DEPENDENCY VERIFICATION [MANDATORY]:** Before importing ANY 3rd party library (e.g. `framer-motion`, `lucide-react`, `zustand`), you MUST check `package.json`. If the package is missing, you MUST output the installation command (e.g. `npm install package-name`) before providing the code. **Never** assume a library exists.; State Management:** Use local `useState`/`useReducer` for isolated UI. Use global state strictly for deep prop-drilling avoidance.; TAILWIND VERSION LOCK:** Check `package.json` first. Do not use v4 syntax in v3 projects.; T4 CONFIG GUARD:** For v4, do NOT use `tailwindcss` plugin in `postcss.config.js`. Use `@tailwindcss/postcss` or the Vite plugin.
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### design-taste-frontend
- Path: `C:\Users\jingr\codex-skills\design-taste-frontend\SKILL.md`
- Purpose: Anti-slop frontend skill for landing pages, portfolios, and redesigns. The agent reads the brief, infers the right design direction, and ships interfaces that do not look templated. Real design systems when applicable, audit-first on redesigns, strict pre-flight check.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Reference signals** - URLs they linked, screenshots they pasted, products they named, brands they're competing with.; "Reading this as: B2B SaaS landing for technical buyers, with a Linear-style minimalist language, leaning toward Tailwind utilities + Geist + restrained motion."*; "Reading this as: solo designer portfolio for hiring managers, with an editorial / kinetic-type language, leaning toward native CSS + scroll-driven animation + custom typography."*; "Reading this as: redesign of a public-sector service site, with a trust-first language, leaning toward GOV.UK Frontend or USWDS."*; Baseline:** `8 / 6 / 4`. Use these unless the design read overrides them. Do not ask the user to edit this file - overrides happen conversationally.
- Must-follow rules: INTERACTIVITY ISOLATION:** Any component using Motion, scroll listeners, or pointer physics MUST be an isolated leaf with `'use client'` at the top. Server Components render static layouts only.; Fonts:** Always use `next/font` (Next.js) or self-host with `@font-face` + `font-display: swap`. Never link Google Fonts via `<link>` in production.; Viewport Stability:** NEVER use `h-screen` for full-height Hero sections. ALWAYS use `min-h-[100dvh]` to prevent layout jumping on mobile (iOS Safari address bar).; Grid over Flex-Math:** NEVER use complex flexbox percentage math (`w-[calc(33%-1rem)]`). ALWAYS use CSS Grid (`grid grid-cols-1 md:grid-cols-3 gap-6`).; Palette-rotation rule:** if the previous premium-consumer project you generated used the beige+brass family, this one MUST use a different family. Do not ship the same warm-craft palette twice in a row.; CTA BUTTON WRAP BAN (mandatory):** Button text MUST fit on one line at desktop. If a label like "VIEW SELECTED WORK" wraps to 2 or 3 lines, the button is broken. Fix by EITHER shortening the label (3 words max for primary CTAs, ideally 1-2) OR widening the button (do not artificially constrain `max-width` on CTAs). Wrapped CTAs at desktop are a Pre-Flight Fail.
- Output/acceptance signals: Reference signals** - URLs they linked, screenshots they pasted, products they named, brands they're competing with.; Serif is **very discouraged as the default font for any project.** "It feels creative / premium / editorial" is NOT a reason to reach for serif. The agent's default mental model that "creative brief = serif" is the single most-tested AI tell in production rounds.; THE LILA RULE:** The "AI Purple / Blue glow" aesthetic is discouraged as a default. No automatic purple button glows, no random neon gradients. Use neutral bases (Zinc / Slate / Stone) with high-contrast singular accents (Emerald, Electric Blue, Deep Rose, Burnt Orange, etc.).; Black and Tan:** true off-black + warm tan, sharp contrast, no beige; BUTTON CONTRAST CHECK (mandatory, a11y):** Before shipping any button, verify the button text is readable against the button background. White button + white text, `bg-white` CTA with `text-white` label, transparent button against the page background with no border ? all banned. Audit every CTA: contrast ratio WCAG AA min (4.5:1 for body, 3:1 for large text 18px+). Same rule applies to ghost buttons over photographic backgrounds (use a backdrop, scrim, or stroke).
- Forbidden/caution signals: Baseline:** `8 / 6 / 4`. Use these unless the design read overrides them. Do not ask the user to edit this file - overrides happen conversationally.; Honesty rule:** if the brief reads as one of the systems above, install and use the **official** package. Do not recreate its CSS by hand. Do not import a system's tokens but then override 90% of them.; One system per project.** Do not mix Fluent React with Carbon in the same tree. Do not import shadcn/ui components into a Material 3 app.; For v4: do NOT use `tailwindcss` plugin in `postcss.config.js`. Use `@tailwindcss/postcss` or the Vite plugin.; Fonts:** Always use `next/font` (Next.js) or self-host with `@font-face` + `font-display: swap`. Never link Google Fonts via `<link>` in production.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### distill
- Path: `C:\Users\jingr\codex-skills\distill\SKILL.md`
- Purpose: distill
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### emil-design-eng
- Path: `C:\Users\jingr\codex-skills\emil-design-eng\SKILL.md`
- Purpose: This skill encodes Emil Kowalski's philosophy on UI polish, component design, animation decisions, and the invisible details that make software feel great.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Never animate keyboard-initiated actions.** These actions are repeated hundreds of times daily. Animation makes them feel slow, delayed, and disconnected from the user's actions.; Never use ease-in for UI animations.** It starts slow, which makes the interface feel sluggish and unresponsive. A dropdown with `ease-in` at 300ms _feels_ slower than `ease-out` at the same 300ms, because ease-in delays the initial movement ? the exact moment the user is watching most closely.; Good defaults matter more than options.** Ship beautiful out of the box. Most users never customize. The default easing, timing, and visual design should be excellent.
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### fixing-accessibility
- Path: `C:\Users\jingr\codex-skills\fixing-accessibility\SKILL.md`
- Purpose: Audit and fix HTML accessibility issues including ARIA labels, keyboard navigation, focus management, color contrast, and form errors. Use when adding interactive controls, forms, dialogs, or reviewing WCAG compliance.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: `/fixing-accessibility <file>`; do not add aria when native semantics already solve the problem; prefer native HTML before adding aria
- Must-follow rules: every interactive control must have an accessible name; icon-only buttons must have aria-label or aria-labelledby; every input, select, and textarea must be labeled; links must have meaningful text (no ?click here?); decorative icons must be aria-hidden; all interactive elements must be reachable by Tab
- Output/acceptance signals: building forms, validation, error states, helper text; ensure sufficient contrast for text and icons
- Forbidden/caution signals: do not use div or span as buttons without full keyboard support; do not use tabindex greater than 0; do not skip heading levels; do not remove focus outlines without a visible replacement; avoid autoplaying media with sound
- Potential conflicts: No major conflict identified; use within task scope.

### fixing-metadata
- Path: `C:\Users\jingr\codex-skills\fixing-metadata\SKILL.md`
- Purpose: >
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: keep titles short and readable, avoid stuffing; manifest must be valid and referenced when used
- Must-follow rules: metadata must be deterministic, no random or unstable values; every page must have safe defaults for title and description; every page must have a title; descriptions must be plain text, no markdown or quote spam; canonical must point to the preferred URL for the page; robots meta must match actual access intent
- Output/acceptance signals: building shared SEO components or layout metadata defaults; verify social cards on a real URL, not localhost
- Forbidden/caution signals: define metadata in one place per page, avoid competing systems; do not emit duplicate title, description, canonical, or robots tags; keep titles short and readable, avoid stuffing; set theme-color intentionally to avoid mismatched UI chrome; do not add JSON-LD unless it clearly maps to real page content
- Potential conflicts: No major conflict identified; use within task scope.

### fixing-motion-performance
- Path: `C:\Users\jingr\codex-skills\fixing-motion-performance\SKILL.md`
- Purpose: Audit and fix animation performance issues including layout thrashing, compositor properties, scroll-linked motion, and blur effects. Use when animations stutter, transitions jank, or reviewing CSS/JS animation performance.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: `/fixing-motion-performance <file>`; do not interleave layout reads and writes in the same frame; batch all DOM reads before writes; do not read layout repeatedly during an animation; prefer opacity and translate before blur
- Must-follow rules: scroll-linked motion must not trigger continuous layout or paint on large surfaces; avoid view transitions when interruption or cancellation is required
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: do not interleave layout reads and writes in the same frame; do not animate layout continuously on large or meaningful surfaces; do not drive animation from scrollTop, scrollY, or scroll events; do not mix multiple animation systems that each measure or mutate layout; do not read layout repeatedly during an animation
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### frontend-design
- Path: `C:\Users\jingr\codex-skills\frontend-design\SKILL.md`
- Purpose: Guidance for distinctive, intentional visual design when building new UI or reshaping an existing one. Helps with aesthetic direction, typography, and making choices that don't read as templated defaults.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.

### design-taste-frontend
- Path: `C:\Users\jingr\codex-skills\frontend-skills\design-taste-frontend\SKILL.md`
- Purpose: Anti-slop frontend skill for landing pages, portfolios, and redesigns. The agent reads the brief, infers the right design direction, and ships interfaces that do not look templated. Real design systems when applicable, audit-first on redesigns, strict pre-flight check.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Reference signals** - URLs they linked, screenshots they pasted, products they named, brands they're competing with.; "Reading this as: B2B SaaS landing for technical buyers, with a Linear-style minimalist language, leaning toward Tailwind utilities + Geist + restrained motion."*; "Reading this as: solo designer portfolio for hiring managers, with an editorial / kinetic-type language, leaning toward native CSS + scroll-driven animation + custom typography."*; "Reading this as: redesign of a public-sector service site, with a trust-first language, leaning toward GOV.UK Frontend or USWDS."*; Baseline:** `8 / 6 / 4`. Use these unless the design read overrides them. Do not ask the user to edit this file - overrides happen conversationally.
- Must-follow rules: INTERACTIVITY ISOLATION:** Any component using Motion, scroll listeners, or pointer physics MUST be an isolated leaf with `'use client'` at the top. Server Components render static layouts only.; Fonts:** Always use `next/font` (Next.js) or self-host with `@font-face` + `font-display: swap`. Never link Google Fonts via `<link>` in production.; Viewport Stability:** NEVER use `h-screen` for full-height Hero sections. ALWAYS use `min-h-[100dvh]` to prevent layout jumping on mobile (iOS Safari address bar).; Grid over Flex-Math:** NEVER use complex flexbox percentage math (`w-[calc(33%-1rem)]`). ALWAYS use CSS Grid (`grid grid-cols-1 md:grid-cols-3 gap-6`).; Palette-rotation rule:** if the previous premium-consumer project you generated used the beige+brass family, this one MUST use a different family. Do not ship the same warm-craft palette twice in a row.; CTA BUTTON WRAP BAN (mandatory):** Button text MUST fit on one line at desktop. If a label like "VIEW SELECTED WORK" wraps to 2 or 3 lines, the button is broken. Fix by EITHER shortening the label (3 words max for primary CTAs, ideally 1-2) OR widening the button (do not artificially constrain `max-width` on CTAs). Wrapped CTAs at desktop are a Pre-Flight Fail.
- Output/acceptance signals: Reference signals** - URLs they linked, screenshots they pasted, products they named, brands they're competing with.; Serif is **very discouraged as the default font for any project.** "It feels creative / premium / editorial" is NOT a reason to reach for serif. The agent's default mental model that "creative brief = serif" is the single most-tested AI tell in production rounds.; THE LILA RULE:** The "AI Purple / Blue glow" aesthetic is discouraged as a default. No automatic purple button glows, no random neon gradients. Use neutral bases (Zinc / Slate / Stone) with high-contrast singular accents (Emerald, Electric Blue, Deep Rose, Burnt Orange, etc.).; Black and Tan:** true off-black + warm tan, sharp contrast, no beige; BUTTON CONTRAST CHECK (mandatory, a11y):** Before shipping any button, verify the button text is readable against the button background. White button + white text, `bg-white` CTA with `text-white` label, transparent button against the page background with no border ? all banned. Audit every CTA: contrast ratio WCAG AA min (4.5:1 for body, 3:1 for large text 18px+). Same rule applies to ghost buttons over photographic backgrounds (use a backdrop, scrim, or stroke).
- Forbidden/caution signals: Baseline:** `8 / 6 / 4`. Use these unless the design read overrides them. Do not ask the user to edit this file - overrides happen conversationally.; Honesty rule:** if the brief reads as one of the systems above, install and use the **official** package. Do not recreate its CSS by hand. Do not import a system's tokens but then override 90% of them.; One system per project.** Do not mix Fluent React with Carbon in the same tree. Do not import shadcn/ui components into a Material 3 app.; For v4: do NOT use `tailwindcss` plugin in `postcss.config.js`. Use `@tailwindcss/postcss` or the Vite plugin.; Fonts:** Always use `next/font` (Next.js) or self-host with `@font-face` + `font-display: swap`. Never link Google Fonts via `<link>` in production.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### impeccable
- Path: `C:\Users\jingr\codex-skills\frontend-skills\impeccable\SKILL.md`
- Purpose: Use when the user wants to design, redesign, shape, critique, audit, polish, clarify, distill, harden, optimize, adapt, animate, colorize, extract, or otherwise improve a frontend interface. Covers websites, landing pages, dashboards, product UI, app shells, components, forms, settings, onboarding, and empty states. Handles UX review, visual hierarchy, information architecture, cognitive load, accessibility, performance, responsive behavior, theming, anti-patterns, typography, fonts, spacing, layout, alignment, color, motion, micro-interactions, UX copy, error states, edge cases, i18n, and reusable design systems or tokens. Also use for bland designs that need to become bolder or more delightful, loud designs that should become quieter, live browser iteration on UI elements, or ambitious visual effects that should feel technically extraordinary. Not for backend-only or non-UI tasks.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Run `node .agents/skills/impeccable/scripts/context.mjs` once per session. If you've already seen its output in this conversation, do not re-run it. The script either prints the project's PRODUCT.md (and DESIGN.md when present) as a markdown block, or tells you it's missing. Follow whatever it prints. **If it reports `NO_PRODUCT_MD`, stop and follow `reference/init.md` before doing anything else.** If the output ends with an `UPDATE_AVAILABLE` directive, follow it (ask the user once about updating, then continue). It never blocks the current task.; If the user invoked a sub-command (`craft`, `shape`, `audit`, `polish`, ...), you MUST read `reference/<command>.md` next. Non-optional. The reference defines the command's flow; without it you will skip steps the user expects.; Familiarize yourself with any existing design system, conventions, and components in the code. Read at least one project file (CSS / tokens / theme / a representative component or page). **Required even when you've loaded a sub-command reference in step 2.** Don't reinvent the wheel; use what's there when it works, branch out when the UX wins.; Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Cap font-family count at 3 (display + body + optional mono). More than 3 reads as indecision, not richness. One well-tuned family with weight contrast usually beats three competing typefaces.
- Must-follow rules: If the user invoked a sub-command (`craft`, `shape`, `audit`, `polish`, ...), you MUST read `reference/<command>.md` next. Non-optional. The reference defines the command's flow; without it you will skip steps the user expects.; Familiarize yourself with any existing design system, conventions, and components in the code. Read at least one project file (CSS / tokens / theme / a representative component or page). **Required even when you've loaded a sub-command reference in step 2.** Don't reinvent the wheel; use what's there when it works, branch out when the UX wins.; Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Cards are the lazy answer. Use them only when they're truly the best affordance. Nested cards are always wrong.; Reveal animations must enhance an already-visible default. Don't gate content visibility on a class-triggered transition; transitions pause on hidden tabs and headless renderers, so the reveal never fires and the section ships blank.
- Output/acceptance signals: Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Hierarchy through scale + weight contrast (?1.25 ratio between steps). Avoid flat scales.; Cap font-family count at 3 (display + body + optional mono). More than 3 reads as indecision, not richness. One well-tuned family with weight contrast usually beats three competing typefaces.; Don't pair fonts that are similar but not identical (two geometric sans-serifs, two humanist sans-serifs). Pair on a contrast axis (serif + sans, geometric + humanist) or use one family in multiple weights.; Build a semantic z-index scale (dropdown ? sticky ? modal-backdrop ? modal ? toast ? tooltip). Never arbitrary values like 999 or 9999.
- Forbidden/caution signals: Run `node .agents/skills/impeccable/scripts/context.mjs` once per session. If you've already seen its output in this conversation, do not re-run it. The script either prints the project's PRODUCT.md (and DESIGN.md when present) as a markdown block, or tells you it's missing. Follow whatever it prints. **If it reports `NO_PRODUCT_MD`, stop and follow `reference/init.md` before doing anything else.** If the output ends with an `UPDATE_AVAILABLE` directive, follow it (ask the user once about updating, then continue). It never blocks the current task.; Hierarchy through scale + weight contrast (?1.25 ratio between steps). Avoid flat scales.; Build a semantic z-index scale (dropdown ? sticky ? modal-backdrop ? modal ? toast ? tooltip). Never arbitrary values like 999 or 9999.; Staggering the items within one list is legitimate. The tell is the uniform reflex (one identical entrance applied to every section), not motion itself; each reveal should fit what it reveals. Suppressing the reflex is never a reason to ship a page with no motion at all.; Reveal animations must enhance an already-visible default. Don't gate content visibility on a class-triggered transition; transitions pause on hidden tabs and headless renderers, so the reveal never fires and the section ships blank.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### frontend-slides
- Path: `C:\Users\jingr\codex-skills\frontend-slides\plugins\frontend-slides\skills\frontend-slides\SKILL.md`
- Purpose: Create stunning, animation-rich HTML presentations from scratch or by converting PowerPoint files. Use when the user wants to build a presentation, convert a PPT/PPTX to web, or create slides for a talk/pitch. Helps non-designers discover their aesthetic through visual exploration rather than abstract choices.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Zero Dependencies** ? Single HTML files with inline CSS/JS. No npm, no build tools.; When generating, read `viewport-base.css` and include its full contents in every presentation.**; Mode B: PPT Conversion** ? Convert a .pptx file. Go to Phase 4.; Mode C: Enhancement** ? Improve an existing HTML presentation. Read it, understand it, enhance. **Follow Mode C modification rules below.**; Before adding content:** Count existing elements, check against density limits
- Must-follow rules: Distinctive Design** ? No generic "AI slop." Every presentation must feel custom-crafted.; The stage scales uniformly to fit the viewport. It may letterbox/pillarbox; it must not re-layout content.; Slide visibility must be controlled by `.active` / `.visible` using `visibility`, `opacity`, and `pointer-events` from `viewport-base.css`. Do not use `display: none` / `display: block` for slide switching; later layout classes such as `.slide-content { display: flex; }` can override them and make every slide visible at once.; Keep it feasible for a full deck. The preview must imply a design system that can expand into section, content, quote, comparison, and closing slides.; Every style preview must look like a real first slide from the user's deck, not a diagnostic card.; Local images/videos must travel with the HTML.** The deploy script auto-detects files referenced via `src="..."` in the HTML and bundles them. But if the presentation references files via CSS `background-image` or unusual paths, those may be missed. **Before deploying, verify:** open the deployed URL and check that all images load. If any are broken, the safest fix is to put the HTML and all its assets into a single folder and deploy the folder instead of a standalone HTML file.
- Output/acceptance signals: Zero Dependencies** ? Single HTML files with inline CSS/JS. No npm, no build tools.; The wildcard may be either a second bold template or a self-generated custom design. Choose whichever creates the strongest, most useful contrast for the user's occasion, audience, mood, and content.; Use fixed 1920?1080 stage rules and pass the same preview authenticity checks as every other option.; After generating, verify both content overflow and panel overlap in rendered browser screenshots. `scrollHeight` checks alone are not enough because grid panels can visually cover each other.; Local images/videos must travel with the HTML.** The deploy script auto-detects files referenced via `src="..."` in the HTML and bundles them. But if the presentation references files via CSS `background-image` or unusual paths, those may be missed. **Before deploying, verify:** open the deployed URL and check that all images load. If any are broken, the safest fix is to put the HTML and all its assets into a single folder and deploy the folder instead of a standalone HTML file.
- Forbidden/caution signals: Typography: Choose fonts that are beautiful, unique, and interesting. Avoid generic fonts like Arial and Inter; opt instead for distinctive choices that elevate the frontend's aesthetics.; Do not use responsive breakpoints to rearrange slide content for phones.; Slide visibility must be controlled by `.active` / `.visible` using `visibility`, `opacity`, and `pointer-events` from `viewport-base.css`. Do not use `display: none` / `display: block` for slide switching; later layout classes such as `.slide-content { display: flex; }` can override them and make every slide visible at once.; Never negate CSS functions directly (`-clamp()`, `-min()`, `-max()` are silently ignored) ? use `calc(-1 * clamp(...))` instead; When adding images to existing slides:** Move image to a new slide or reduce other content first. Never add images without checking if existing content already fills the 1920?1080 slide stage.
- Potential conflicts: No major conflict identified; use within task scope.

### frontend-slides
- Path: `C:\Users\jingr\codex-skills\frontend-slides\SKILL.md`
- Purpose: Create stunning, animation-rich HTML presentations from scratch or by converting PowerPoint files. Use when the user wants to build a presentation, convert a PPT/PPTX to web, or create slides for a talk/pitch. Helps non-designers discover their aesthetic through visual exploration rather than abstract choices.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Zero Dependencies** ? Single HTML files with inline CSS/JS. No npm, no build tools.; When generating, read `viewport-base.css` and include its full contents in every presentation.**; Mode B: PPT Conversion** ? Convert a .pptx file. Go to Phase 4.; Mode C: Enhancement** ? Improve an existing HTML presentation. Read it, understand it, enhance. **Follow Mode C modification rules below.**; Before adding content:** Count existing elements, check against density limits
- Must-follow rules: Distinctive Design** ? No generic "AI slop." Every presentation must feel custom-crafted.; The stage scales uniformly to fit the viewport. It may letterbox/pillarbox; it must not re-layout content.; Slide visibility must be controlled by `.active` / `.visible` using `visibility`, `opacity`, and `pointer-events` from `viewport-base.css`. Do not use `display: none` / `display: block` for slide switching; later layout classes such as `.slide-content { display: flex; }` can override them and make every slide visible at once.; Keep it feasible for a full deck. The preview must imply a design system that can expand into section, content, quote, comparison, and closing slides.; Every style preview must look like a real first slide from the user's deck, not a diagnostic card.; Local images/videos must travel with the HTML.** The deploy script auto-detects files referenced via `src="..."` in the HTML and bundles them. But if the presentation references files via CSS `background-image` or unusual paths, those may be missed. **Before deploying, verify:** open the deployed URL and check that all images load. If any are broken, the safest fix is to put the HTML and all its assets into a single folder and deploy the folder instead of a standalone HTML file.
- Output/acceptance signals: Zero Dependencies** ? Single HTML files with inline CSS/JS. No npm, no build tools.; The wildcard may be either a second bold template or a self-generated custom design. Choose whichever creates the strongest, most useful contrast for the user's occasion, audience, mood, and content.; Use fixed 1920?1080 stage rules and pass the same preview authenticity checks as every other option.; After generating, verify both content overflow and panel overlap in rendered browser screenshots. `scrollHeight` checks alone are not enough because grid panels can visually cover each other.; Local images/videos must travel with the HTML.** The deploy script auto-detects files referenced via `src="..."` in the HTML and bundles them. But if the presentation references files via CSS `background-image` or unusual paths, those may be missed. **Before deploying, verify:** open the deployed URL and check that all images load. If any are broken, the safest fix is to put the HTML and all its assets into a single folder and deploy the folder instead of a standalone HTML file.
- Forbidden/caution signals: Typography: Choose fonts that are beautiful, unique, and interesting. Avoid generic fonts like Arial and Inter; opt instead for distinctive choices that elevate the frontend's aesthetics.; Do not use responsive breakpoints to rearrange slide content for phones.; Slide visibility must be controlled by `.active` / `.visible` using `visibility`, `opacity`, and `pointer-events` from `viewport-base.css`. Do not use `display: none` / `display: block` for slide switching; later layout classes such as `.slide-content { display: flex; }` can override them and make every slide visible at once.; Never negate CSS functions directly (`-clamp()`, `-min()`, `-max()` are silently ignored) ? use `calc(-1 * clamp(...))` instead; When adding images to existing slides:** Move image to a new slide or reduce other content first. Never add images without checking if existing content already fills the 1920?1080 slide stage.
- Potential conflicts: No major conflict identified; use within task scope.

### frontend-ui-engineering
- Path: `C:\Users\jingr\codex-skills\frontend-ui-engineering\SKILL.md`
- Purpose: Builds production-quality UIs. Use when building or modifying user-facing interfaces. Use when creating components, implementing layouts, managing state, or when the output needs to look and feel production-quality rather than AI-generated.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: [ ] Screen reader can convey the page's content and structure
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Building new UI components or pages; Avoid prop drilling deeper than 3 levels.** If you're passing props through components that don't use them, introduce context or restructure the component tree.; Ensure sufficient contrast (4.5:1 for normal text, 3:1 for large text); No keyboard navigation testing
- Forbidden/caution signals: Avoid prop drilling deeper than 3 levels.** If you're passing props through components that don't use them, introduce context or restructure the component tree.
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### full-output-enforcement
- Path: `C:\Users\jingr\codex-skills\full-output-enforcement\SKILL.md`
- Purpose: Overrides default LLM truncation behavior. Enforces complete code generation, bans placeholder patterns, and handles token-limit splits cleanly. Apply to any task requiring exhaustive, unabridged output.
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Scope** ? Read the full request. Count how many distinct deliverables are expected (files, functions, sections, answers). Lock that number.; Cross-check** ? Before output, re-read the original request. Compare your deliverable count against the scope count. If anything is missing, add it before responding.; Write at full quality up to a clean breakpoint (end of a function, end of a file, end of a section).
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Build** ? Generate every deliverable completely. No partial drafts, no "you can extend this later."
- Forbidden/caution signals: Do not compress remaining sections to squeeze them in.; Do not skip ahead to a conclusion.
- Potential conflicts: Full-output pressure may conflict with staged incremental work.

### generating-sounds-with-ai
- Path: `C:\Users\jingr\codex-skills\generating-sounds-with-ai\SKILL.md`
- Purpose: Audit Web Audio API code for sound synthesis best practices. Use when reviewing procedural audio, implementing UI sounds, or checking audio parameter quality. Outputs file:line findings.
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### gpt-taste
- Path: `C:\Users\jingr\codex-skills\gpt-taste\SKILL.md`
- Purpose: Elite UX/UI & Advanced GSAP Motion Engineer. Enforces Python-driven true randomization for layout variance, strict AIDA page structure, wide editorial typography (bans 6-line wraps), gapless bento grids, strict GSAP ScrollTriggers (pinning, stacking, scrubbing), inline micro-images, and massive section spacing.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: SPACING RULE:** Add huge vertical padding between all major sections (e.g., `py-32 md:py-48`). Sections must feel like distinct, cinematic chapters. Do not cramp elements together.; The Container Width Fix:** You MUST use ultra-wide containers for the H1 (e.g., `max-w-5xl`, `max-w-6xl`, `w-full`). Allow the words to flow horizontally.; The Line Limit:** The H1 MUST NEVER exceed 2 to 3 lines. 4, 5, or 6 lines is a catastrophic failure. Make the font size smaller (`clamp(3rem, 5vw, 5.5rem)`) and the container wider to ensure this.; Button Contrast:** Buttons must be perfectly legible. Dark background = white text. Light background = dark text. Invisible text is a failure.; Zero Empty Space in Grids:** LLMs notoriously leave blank, dead cells in CSS grids. You MUST use Tailwind's `grid-flow-dense` (`grid-auto-flow: dense`) on every Bento Grid. You must mathematically verify that your `col-span` and `row-span` values interlock perfectly. No grid shall have a missing corner or empty void.; Hover Physics:** Every clickable card and image must react. Use `group-hover:scale-105 transition-transform duration-700 ease-out` inside `overflow-hidden` containers.
- Output/acceptance signals: Action (Footer/Pricing):** Massive, high-contrast CTA and clean footer links.; Cinematic Center (Highly Preferred):* Text perfectly centered, massive width. Below the text, exactly two high-contrast CTAs. Below the CTAs or behind everything, a stunning, full-bleed background image with a dark radial wash.; Button Contrast:** Buttons must be perfectly legible. Dark background = white text. Light background = dark text. Invisible text is a failure.; Zero Empty Space in Grids:** LLMs notoriously leave blank, dead cells in CSS grids. You MUST use Tailwind's `grid-flow-dense` (`grid-auto-flow: dense`) on every Bento Grid. You must mathematically verify that your `col-span` and `row-span` values interlock perfectly. No grid shall have a missing corner or empty void.; Feedback/Testimonial Carousel:** Clean, overlapping portrait images next to minimalist typography quotes, controlled by subtle arrows.
- Forbidden/caution signals: Typography Stack (Satoshi, Cabinet Grotesk, Outfit, or Geist. NEVER Inter); SPACING RULE:** Add huge vertical padding between all major sections (e.g., `py-32 md:py-48`). Sections must feel like distinct, cinematic chapters. Do not cramp elements together.; The Line Limit:** The H1 MUST NEVER exceed 2 to 3 lines. 4, 5, or 6 lines is a catastrophic failure. Make the font size smaller (`clamp(3rem, 5vw, 5.5rem)`) and the container wider to ensure this.; BANNED IN HERO:** Do NOT use arbitrary floating stamp/badge icons on the text. Do NOT use pill-tags under the hero. Do NOT place raw data/stats in the hero.; Card Restraint:** Do not use too many cards. 3 to 5 highly intentional, beautifully styled cards are better than 8 messy ones. Fill them with a mix of large imagery, dense typography, or CSS effects.
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### harden
- Path: `C:\Users\jingr\codex-skills\harden\SKILL.md`
- Purpose: harden
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### high-end-visual-design
- Path: `C:\Users\jingr\codex-skills\high-end-visual-design\SKILL.md`
- Purpose: Teaches the AI to design like a high-end agency. Defines the exact fonts, spacing, shadows, card structures, and animations that make a website feel expensive. Blocks all the common defaults that make AI designs look cheap or generic.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: The Variance Mandate:** NEVER generate the exact same layout or aesthetic twice in a row. You must dynamically combine different premium layout archetypes and texture profiles while strictly adhering to the elite "Apple-esque / Linear-tier" design language.; [ ] The overall impression reads as "$150k agency build", not "template with nice fonts"
- Must-follow rules: Objective:** You engineer $150k+ agency-level digital experiences, not just websites. Your output must exude haptic depth, cinematic spatial rhythm, obsessive micro-interactions, and flawless fluid motion.; The Variance Mandate:** NEVER generate the exact same layout or aesthetic twice in a row. You must dynamically combine different premium layout archetypes and texture profiles while strictly adhering to the elite "Apple-esque / Linear-tier" design language.; Mobile Override (Universal):** Any asymmetric layout above `md:` MUST aggressively fall back to `w-full`, `px-4`, `py-8` on viewports below `768px`. Never use `h-screen` for full-height sections ? always use `min-h-[100dvh]` to prevent iOS Safari viewport jumping.; Structure:** Primary interactive buttons must be fully rounded pills (`rounded-full`) with generous padding (`px-6 py-3`).; The "Button-in-Button" Trailing Icon:** If a button has an arrow (`?`), it NEVER sits naked next to the text. It must be nested inside its own distinct circular wrapper (e.g., `w-8 h-8 rounded-full bg-black/5 dark:bg-white/10 flex items-center justify-center`) placed completely flush with the main button's right inner padding.; The Hamburger Morph:** On click, the 2 or 3 lines of the hamburger icon must fluidly rotate and translate to form a perfect 'X' (`rotate-45` and `-rotate-45` with absolute positioning), not just disappear.
- Output/acceptance signals: Editorial Luxury (Lifestyle / Real Estate / Agency):** Warm creams (`#FDFBF7`), muted sage, or deep espresso tones. High-contrast Variable Serif fonts for massive headings. Subtle CSS noise/film-grain overlay (`opacity-[0.03]`) for a physical paper feel.; [ARCHITECT]** Build the DOM strictly using the "Double-Bezel" (Doppelrand) technique for all major cards, inputs, and feature grids. Use exaggerated squircle radii (`rounded-[2rem]`).; [ ] The overall impression reads as "$150k agency build", not "template with nice fonts"
- Forbidden/caution signals: The Variance Mandate:** NEVER generate the exact same layout or aesthetic twice in a row. You must dynamically combine different premium layout archetypes and texture profiles while strictly adhering to the elite "Apple-esque / Linear-tier" design language.; Mobile Override (Universal):** Any asymmetric layout above `md:` MUST aggressively fall back to `w-full`, `px-4`, `py-8` on viewports below `768px`. Never use `h-screen` for full-height sections ? always use `min-h-[100dvh]` to prevent iOS Safari viewport jumping.; The "Button-in-Button" Trailing Icon:** If a button has an arrow (`?`), it NEVER sits naked next to the text. It must be nested inside its own distinct circular wrapper (e.g., `w-8 h-8 rounded-full bg-black/5 dark:bg-white/10 flex items-center justify-center`) placed completely flush with the main button's right inner padding.; Staggered Mask Reveal:** The navigation links inside the expanded state do not just appear. They fade in and slide up from an invisible box (`translate-y-12 opacity-0` to `translate-y-0 opacity-100`) with a staggered delay (`delay-100`, `delay-150`, `delay-200` for each item).; Use the `group` utility. On hover, do not just change the background color.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### image-to-code
- Path: `C:\Users\jingr\codex-skills\image-to-code\SKILL.md`
- Purpose: Elite website image-to-code skill for Codex. For visually important web tasks, it must first generate the design image(s) itself, deeply analyze them, then implement the website to match them as closely as possible. In Codex, it must prefer large, readable, section-specific images instead of tiny compressed boards, generate fresh standalone images for sections or detail views instead of cropping old ones, avoid lazy under-generation, avoid cards-inside-cards-inside-cards UI, and keep the hero clean, spacious, readable, and visible on a small laptop.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: text that becomes too small to read; readable; Prefer readability over squeezing too much into one image.; describe a website without first generating the visual reference when generation is available; rely on memory of ?good frontend taste? instead of producing the actual reference
- Must-follow rules: the hero must feel like a strong opening scene; the main headline must feel short and powerful; image usage must feel deliberate; all image moments must still feel like one coherent design world
- Output/acceptance signals: faithful enough to build from; a closer render for testimonials; split testimonial quote wall; prioritize negative space and contrast; weak headline-to-subheadline contrast
- Forbidden/caution signals: Do not be lazy with image count.; do not overcrowd the first viewport; do not allow long wrapped hero headlines; avoid stuffing the hero with pills, fake stats, badges, tiny logos, and nonsense detail; avoid extra micro-labels, control tags, system markers, or decorative utility text that does not meaningfully help the hero
- Potential conflicts: No major conflict identified; use within task scope.

### imagegen-frontend-mobile
- Path: `C:\Users\jingr\codex-skills\imagegen-frontend-mobile\SKILL.md`
- Purpose: Elite mobile app image-generation skill for creating premium, app-native screen concepts and flows. Designed for iOS, Android, and cross-platform mobile products. Prioritizes clean hierarchy, comfortably readable text, strong multi-screen consistency, controlled color palettes, non-generic creative direction, textured surfaces, image-led composition, tasteful custom iconography, and clean phone mockup framing. By default, screens should be shown inside a subtle premium iPhone or similar phone mockup with a visible frame, while the main focus stays on the app content itself. This skill generates images only. It does not write code.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: profile screens; readable; tiny unreadable text; TEXT_READABILITY_PRIORITY: 10; If the user says "creative social app", increase visual variance and image creativity without sacrificing readability.
- Must-follow rules: Always keep the color palette clean, controlled, and intentional.; text must stay readable; different screens can use different images, but they must still belong to one product world
- Output/acceptance signals: premium but broadly buildable visual language; warm neutral palette + sharp dark contrast; enough contrast against the background; strong title/body/label contrast
- Forbidden/caution signals: Do not be lazy with screen count.; Avoid generic color choices.; Do not force every app into ultra-simple minimalism.; do not let the phone touch the canvas edges; do not use awkwardly cropped device frames
- Potential conflicts: No major conflict identified; use within task scope.

### imagegen-frontend-web
- Path: `C:\Users\jingr\codex-skills\imagegen-frontend-web\SKILL.md`
- Purpose: Elite frontend image-direction skill for generating premium, conversion-aware website design references. CRITICAL OUTPUT RULE ? generate ONE separate horizontal image FOR EVERY section. A landing page with 8 sections produces 8 images. Never compress multiple sections into one image. Enforces composition variety (not always left-text / right-image), background-image freedom, varied CTAs, varied hero scales (giant / mid / mini minimalist), narrative concept spine, second-read moments, and a single consistent palette across all images. Optimized for landing pages, marketing sites, and product comps that developers or coding models can accurately recreate.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: "creative" that is actually messy and unreadable; readable; clearly usable as a frontend reference; Adaptation priority**: the user's brief always overrides defaults. Read the prompt carefully, then adjust dials, hero scale, background mode, gradient use, and composition variety to match ? never force a recipe that contradicts the brief.; Full-bleed image background with tonal overlay (text remains highly readable)
- Must-follow rules: Generate one separate horizontal image PER section. Always. No exceptions.**; Adaptation priority**: the user's brief always overrides defaults. Read the prompt carefully, then adjust dials, hero scale, background mode, gradient use, and composition variety to match ? never force a recipe that contradicts the brief.; Skip the "must include full-bleed" rule; the hero must feel like a strong opening scene; the main headline must feel short and powerful; Always horizontal (16:9, 16:10, or 21:9 depending on density)
- Output/acceptance signals: Gradients: skip or use only the softest tonal gradient; Strong typography contrast; Full-bleed cinematic imagery with proper contrast control; Split Testimonial Quote Wall; Stage / spotlight ? theatrical contrast, performer + audience framing
- Forbidden/caution signals: Adaptation priority**: the user's brief always overrides defaults. Read the prompt carefully, then adjust dials, hero scale, background mode, gradient use, and composition variety to match ? never force a recipe that contradicts the brief.; Bias toward stronger visual concepts, not safe layouts ? but never against the brief.; Vary composition: do not default to "text left, image right". Move text to bottom-left, center, top-right, etc. across sections.; Keep sections breathable. Do not over-pack the page.; Gradients: subtle, never competing with product
- Potential conflicts: No major conflict identified; use within task scope.

### impeccable
- Path: `C:\Users\jingr\codex-skills\impeccable\SKILL.md`
- Purpose: Use when the user wants to design, redesign, shape, critique, audit, polish, clarify, distill, harden, optimize, adapt, animate, colorize, extract, or otherwise improve a frontend interface. Covers websites, landing pages, dashboards, product UI, app shells, components, forms, settings, onboarding, and empty states. Handles UX review, visual hierarchy, information architecture, cognitive load, accessibility, performance, responsive behavior, theming, anti-patterns, typography, fonts, spacing, layout, alignment, color, motion, micro-interactions, UX copy, error states, edge cases, i18n, and reusable design systems or tokens. Also use for bland designs that need to become bolder or more delightful, loud designs that should become quieter, live browser iteration on UI elements, or ambitious visual effects that should feel technically extraordinary. Not for backend-only or non-UI tasks.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Run `node .agents/skills/impeccable/scripts/context.mjs` once per session. If you've already seen its output in this conversation, do not re-run it. The script either prints the project's PRODUCT.md (and DESIGN.md when present) as a markdown block, or tells you it's missing. Follow whatever it prints. **If it reports `NO_PRODUCT_MD`, stop and follow `reference/init.md` before doing anything else.** If the output ends with an `UPDATE_AVAILABLE` directive, follow it (ask the user once about updating, then continue). It never blocks the current task.; If the user invoked a sub-command (`craft`, `shape`, `audit`, `polish`, ...), you MUST read `reference/<command>.md` next. Non-optional. The reference defines the command's flow; without it you will skip steps the user expects.; Familiarize yourself with any existing design system, conventions, and components in the code. Read at least one project file (CSS / tokens / theme / a representative component or page). **Required even when you've loaded a sub-command reference in step 2.** Don't reinvent the wheel; use what's there when it works, branch out when the UX wins.; Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Display letter-spacing ? -0.04em. Your default of -0.05 to -0.085em on display H1s makes the letters touch and reads as cramped. -0.02 to -0.03em is plenty for tight grotesque display; -0.04em is the floor.
- Must-follow rules: If the user invoked a sub-command (`craft`, `shape`, `audit`, `polish`, ...), you MUST read `reference/<command>.md` next. Non-optional. The reference defines the command's flow; without it you will skip steps the user expects.; Familiarize yourself with any existing design system, conventions, and components in the code. Read at least one project file (CSS / tokens / theme / a representative component or page). **Required even when you've loaded a sub-command reference in step 2.** Don't reinvent the wheel; use what's there when it works, branch out when the UX wins.; Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Cards are the lazy answer. Use them only when they're truly the best affordance. Nested cards are always wrong.; Reveal animations must enhance an already-visible default. Don't gate content visibility on a class-triggered transition; transitions pause on hidden tabs and headless renderers, so the reveal never fires and the section ships blank.
- Output/acceptance signals: Verify contrast.** Body text must hit ?4.5:1 against its background; large text (?18px or bold ?14px) needs ?3:1. Placeholder text needs the same 4.5:1, not the muted-gray default. The most common failure: muted gray body text on a tinted near-white. If the contrast is even close, bump the body color toward the ink end of the ramp; light gray "for elegance" is the single biggest reason AI designs feel hard to read.; Don't pair fonts that are similar but not identical (two geometric sans-serifs, two humanist sans-serifs). Pair on a contrast axis (serif + sans, geometric + humanist) or use one family in multiple weights.; Build a semantic z-index scale (dropdown ? sticky ? modal-backdrop ? modal ? toast ? tooltip). Never arbitrary values like 999 or 9999.; Motion should be intentional, and not be an afterthought. consider it as part of the build.; Text that overflows its container.** Long heading words plus large clamp scales plus narrow grids cause headline overflow on tablet/mobile. Test the heading copy at every breakpoint; if it overflows, reduce the clamp max or rewrite the copy. The viewport is part of the design.
- Forbidden/caution signals: Run `node .agents/skills/impeccable/scripts/context.mjs` once per session. If you've already seen its output in this conversation, do not re-run it. The script either prints the project's PRODUCT.md (and DESIGN.md when present) as a markdown block, or tells you it's missing. Follow whatever it prints. **If it reports `NO_PRODUCT_MD`, stop and follow `reference/init.md` before doing anything else.** If the output ends with an `UPDATE_AVAILABLE` directive, follow it (ask the user once about updating, then continue). It never blocks the current task.; Build a semantic z-index scale (dropdown ? sticky ? modal-backdrop ? modal ? toast ? tooltip). Never arbitrary values like 999 or 9999.; Staggering the items within one list is legitimate. The tell is the uniform reflex (one identical entrance applied to every section), not motion itself; each reveal should fit what it reveals. Suppressing the reflex is never a reason to ship a page with no motion at all.; Reveal animations must enhance an already-visible default. Don't gate content visibility on a class-triggered transition; transitions pause on hidden tabs and headless renderers, so the reveal never fires and the section ships blank.; The cream / sand / beige body bg is the saturated AI default of 2026.** The whole warm-neutral band (OKLCH L 0.84-0.97, C < 0.06, hue 40-100) reads as cream/sand/paper/parchment regardless of what you call it. Token names like `--paper`, `--cream`, `--sand`, `--bone`, `--flour`, `--linen`, `--parchment`, `--wheat`, `--biscuit`, `--ivory` are tells in themselves. If the brief is "warm, traditional, family-coastal-Italian" or "magazine-warm" or "editorial-restraint", DO NOT translate that into a near-white warm-tinted bg; that's the AI move. Pick: (a) a saturated brand color as the body (terracotta, oxblood, deep ochre, near-black), (b) a true off-white at chroma 0 (or chroma toward the brand's own hue, not toward warmth-by-default), or (c) a darker mid-tone tinted neutral that's clearly the brand's own. "Warmth" in the brand is carried by accent + typography + imagery, not by body bg.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### industrial-brutalist-ui
- Path: `C:\Users\jingr\codex-skills\industrial-brutalist-ui\SKILL.md`
- Purpose: Raw mechanical interfaces fusing Swiss typographic print with military terminal aesthetics. Rigid grids, extreme type scale contrast, utilitarian color, analog degradation effects. For data-heavy dashboards, portfolios, or editorial sites that need to feel like declassified blueprints.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Terminal Green (`#4AF626`):** Optional. Use ONLY for a single specific UI element (e.g., one status indicator or one data readout) ? never as a general text color. If it doesn't serve a clear purpose, omit it entirely.
- Must-follow rules: Implementation Parameters:** Used exceedingly sparingly. Must be subjected to heavy post-processing (halftone filters, 1-bit dithering) to degrade vector perfection and create textural juxtaposition against the clean sans-serifs.; Geometry:** Absolute rejection of `border-radius`. All corners must be exactly 90 degrees to enforce mechanical rigidity.
- Output/acceptance signals: Description:** Advanced proficiency in architecting web interfaces that synthesize mid-century Swiss Typographic design, industrial manufacturing manuals, and retro-futuristic aerospace/military terminal interfaces. This discipline requires absolute mastery over rigid modular grids, extreme typographic scale contrast, purely utilitarian color palettes, and the programmatic simulation of analog degradation (halftones, CRT scanlines, bitmap dithering). The objective is to construct digital environments that project raw functionality, mechanical precision, and high data density, deliberately discarding conventional consumer UI patterns.; Characteristics:** High-contrast light modes (newsprint/off-white substrates). Reliance on monolithic, heavy sans-serif typography. Unforgiving structural grids outlined by visible dividing lines. Aggressive, asymmetric use of negative space punctuated by oversized, viewport-bleeding numerals or letterforms. Heavy use of primary red as an alert/accent color.; Classification:** High-Contrast Serif.; Grid Determinism:** Utilize `display: grid; gap: 1px;` with contrasting parent/child background colors to generate mathematically perfect, razor-thin dividing lines without complex border declarations.
- Forbidden/caution signals: CRITICAL: Choose ONE substrate palette per project and use it consistently. Never mix light and dark substrates within the same interface.**; Background:** `#0A0A0A` or `#121212` (Deactivated CRT. Avoid pure `#000000`).; Terminal Green (`#4AF626`):** Optional. Use ONLY for a single specific UI element (e.g., one status indicator or one data readout) ? never as a general text color. If it doesn't serve a clear purpose, omit it entirely.; The Blueprint Grid:** Strict adherence to CSS Grid architectures. Elements do not float; they are anchored precisely to grid tracks and intersections.
- Potential conflicts: No major conflict identified; use within task scope.

### interaction-design
- Path: `C:\Users\jingr\codex-skills\interaction-design\SKILL.md`
- Purpose: Design and implement microinteractions, motion design, transitions, and user feedback patterns. Use when adding polish to UI interactions, implementing loading states, or creating delightful user experiences.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: ::before,
- Must-follow rules: Reduce Motion Support**: Always respect `prefers-reduced-motion`
- Output/acceptance signals: Building notification and toast systems
- Forbidden/caution signals: Janky Animations**: Avoid animating `width`, `height`, `top`, `left`; Blocking Interactions**: Never prevent user input during animations
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### interface-design
- Path: `C:\Users\jingr\codex-skills\interface-design\SKILL.md`
- Purpose: Craft-first interface design for dashboards, admin panels, SaaS apps, tools, settings pages, data interfaces, and interactive products. Use when designing, building, reviewing, auditing, or refining product UI where visual craft, layout hierarchy, tokens, states, visual direction, image-based references, or design-system consistency matter. Not for marketing pages, landing pages, campaigns, or brand-only work.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Typography feels like a container.** Pick something readable, move on. But typography isn't holding your design ? it IS your design. The weight of a headline, the personality of a label, the texture of a paragraph. These shape how the product feels before anyone reads a word. A bakery management tool and a trading terminal might both need "clean, readable type" ? but the type that's warm and handmade is not the type that's cold and precise. If you're reaching for your usual font, you're not designing.; Token names feel like implementation detail.** But your CSS variables are design decisions. `--ink` and `--parchment` evoke a world. `--gray-700` and `--surface-2` evoke a template. Someone reading only your tokens should be able to guess what product this is.; The test:** Read your proposal. Remove the product name. Could someone identify what this is for? If not, it's generic. Explore deeper.; Before showing the user, look at what you made.**; The token test:** Read your CSS variables out loud. Do they sound like they belong to this product's world, or could they belong to any project?
- Must-follow rules: What must they accomplish?**; Explore domain ? Produce all four required outputs; Propose ? Direction must reference all four
- Output/acceptance signals: Navigation feels like scaffolding.** Build the sidebar, add the links, get to the real work. But navigation isn't around your product ? it IS your product. Where you are, where you can go, what matters most. A page floating in space is a component demo, not software. The navigation teaches people how to think about the space they're in.; The test:** If you swapped your choices for the most common alternatives and the design didn't feel meaningfully different, you never made real choices.; The test:** Read your proposal. Remove the product name. Could someone identify what this is for? If not, it's generic. Explore deeper.; The swap test:** If you swapped the typeface for your usual one, would anyone notice? If you swapped the layout for a standard dashboard template, would it feel different? The places where swapping wouldn't matter are the places you defaulted.; The squint test:** Blur your eyes. Can you still perceive hierarchy? Is anything jumping out harshly? Craft whispers.
- Forbidden/caution signals: The test:** If you swapped your choices for the most common alternatives and the design didn't feel meaningfully different, you never made real choices.; Do not propose any direction until you produce all four:**; Defaults:** 3 obvious choices for this interface type ? visual AND structural. You can't avoid patterns you haven't named.; NEVER produce identical output.** Same sidebar width, same card grid, same metric boxes with icon-left-number-big-label-small every time ? this signals AI-generated immediately. It's forgettable.; Never say:** "I'm in ESTABLISH MODE", "Let me check system.md..."
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.

### layout
- Path: `C:\Users\jingr\codex-skills\layout\SKILL.md`
- Purpose: layout
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### link-purpose
- Path: `C:\Users\jingr\codex-skills\link-purpose\SKILL.md`
- Purpose: link-purpose
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Identify WCAG, semantic HTML, keyboard, focus, contrast, and form issues relevant to $name.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### make-interfaces-feel-better
- Path: `C:\Users\jingr\codex-skills\make-interfaces-feel-better\SKILL.md`
- Purpose: Design engineering principles for making interfaces feel polished. Use when building UI components, reviewing frontend code, implementing animations, hover states, shadows, borders, typography, micro-interactions, enter/exit animations, or any visual detail work. Triggers on UI polish, design details, "make it feel better", "feels off", stagger animations, border radius, optical alignment, font smoothing, tabular numbers, image outlines, box shadows.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: [ ] `will-change` only on transform/opacity/filter, never `all`
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### mastering-animate-presence
- Path: `C:\Users\jingr\codex-skills\mastering-animate-presence\SKILL.md`
- Purpose: Audit Motion/Framer Motion code for AnimatePresence best practices. Use when reviewing exit animations, modals, or presence state. Outputs file:line findings.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### minimalist-ui
- Path: `C:\Users\jingr\codex-skills\minimalist-ui\SKILL.md`
- Purpose: Clean editorial-style interfaces. Warm monochrome palette, typographic contrast, flat bento grids, muted pastels. No gradients, no heavy shadows.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Editorial Serif (Hero Headings & Quotes): Target: `font-family: 'Lyon Text', 'Newsreader', 'Playfair Display', 'Instrument Serif', serif`. Apply tight tracking (`letter-spacing: -0.02em` to `-0.04em`) and tight line-height (`1.1`).
- Must-follow rules: DO NOT use Tailwind's default heavy drop shadows (e.g., `shadow-md`, `shadow-lg`, `shadow-xl`). Shadows must be practically non-existent or heavily customized to be ultra-diffuse and low opacity (< 0.05).; Text Colors: Body text must never be absolute black (`#000000`). Use off-black/charcoal (`#111111` or `#2F3437`) with a generous `line-height` of `1.6` for legibility. Secondary text should be muted gray (`#787774`).; Cards must have exactly `border: 1px solid #EAEAEA`.; Border-radius must be crisp: `8px` or `12px` maximum.; Internal padding must be generous (e.g., `24px` to `40px`).; Background must use the defined Muted Pastels.
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: DO NOT use the "Inter", "Roboto", or "Open Sans" typefaces.; DO NOT use generic, thin-line icon libraries like "Lucide", "Feather", or standard "Heroicons".; DO NOT use Tailwind's default heavy drop shadows (e.g., `shadow-md`, `shadow-lg`, `shadow-xl`). Shadows must be practically non-existent or heavily customized to be ultra-diffuse and low opacity (< 0.05).; DO NOT use primary colored backgrounds for large elements or sections (e.g., no bright blue, green, or red hero sections).; DO NOT use gradients, neon colors, or 3D glassmorphism (beyond subtle navbar blurs).
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### morphing-icons
- Path: `C:\Users\jingr\codex-skills\morphing-icons\SKILL.md`
- Purpose: Build icon components where any icon morphs into any other through SVG line transformation. Use when asked to "create morphing icons", "build icon transitions", "animate between icons", or "transform icons".
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### next-best-practices
- Path: `C:\Users\jingr\codex-skills\next-best-practices\SKILL.md`
- Purpose: Next.js best practices - file conventions, RSC boundaries, data patterns, async APIs, metadata, error handling, route handlers, image/font optimization, bundling
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Project structure and special files; File-based metadata conventions; Polyfills (already included)
- Must-follow rules: Always use `next/image` over `<img>`
- Output/acceptance signals: Generate functions: `generateStaticParams`, `generateMetadata`; Rebuild specific routes with `--debug-build-paths`
- Forbidden/caution signals: `forbidden`, `unauthorized` (auth errors); Avoiding data waterfalls (`Promise.all`, Suspense, preload)
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### next-cache-components
- Path: `C:\Users\jingr\codex-skills\next-cache-components\SKILL.md`
- Purpose: Next.js 16 Cache Components - PPR, use cache directive, cacheLife, cacheTag, updateTag
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Before (`unstable_cache`):**; Revalidation** - Replace `options.revalidate` with `cacheLife({ revalidate: N })` or a built-in profile like `cacheLife('minutes')`.; [use cache Directive](https://nextjs.org/docs/app/api-reference/directives/use-cache); [unstable_cache (legacy)](https://nextjs.org/docs/app/api-reference/functions/unstable_cache)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Build ID** - invalidates all caches on deploy; Non-deterministic values** (`Math.random()`, `Date.now()`) execute once at build time inside `use cache`
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### next-upgrade
- Path: `C:\Users\jingr\codex-skills\next-upgrade\SKILL.md`
- Purpose: Upgrade Next.js to the latest version following official migration guides and codemods
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Detect current version**: Read `package.json` to identify the current Next.js version and related dependencies (React, React DOM, etc.)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Fetch the latest upgrade guide**: Use WebFetch to get the official upgrade documentation:; Run `npm run build` to check for build errors; Run `npm run dev` and test key functionality
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### nuxt
- Path: `C:\Users\jingr\codex-skills\nuxt\SKILL.md`
- Purpose: Nuxt full-stack Vue framework with SSR, auto-imports, and file-based routing. Use when working with Nuxt apps, server routes, useFetch, middleware, or hybrid rendering.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### oklch-skill
- Path: `C:\Users\jingr\codex-skills\oklch-skill\SKILL.md`
- Purpose: OKLCH color space for web projects. Convert hex/rgb/hsl to oklch, generate palettes, check contrast, handle gamut boundaries, and theme with Tailwind v4. Triggers on oklch, color conversion, palette generation, contrast ratio, gamut, display p3, design tokens, hue drift, chroma, dark mode colors.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: Finite gamut.** Not every oklch value maps to a displayable sRGB color. High-chroma values at certain hues will clip ? gamut awareness is required.
- Output/acceptance signals: [accessibility-contrast.md](accessibility-contrast.md) ? APCA and WCAG 2 thresholds, fixing contrast with L, lightness gap guide, hue drift detection
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### optimize
- Path: `C:\Users\jingr\codex-skills\optimize\SKILL.md`
- Purpose: optimize
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### overdrive
- Path: `C:\Users\jingr\codex-skills\overdrive\SKILL.md`
- Purpose: overdrive
- Categories: B_weather_visual
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Full-output pressure may conflict with staged incremental work.

### pinia
- Path: `C:\Users\jingr\codex-skills\pinia\SKILL.md`
- Purpose: Pinia official Vue state management library, type-safe and extensible. Use when defining stores, working with state/getters/actions, or implementing store patterns in Vue apps.
- Categories: A_LifeBoard_core, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Use `@pinia/testing`** for component tests with mocked stores
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### playwright-cli
- Path: `C:\Users\jingr\codex-skills\playwright-cli\SKILL.md`
- Purpose: Automate browser interactions, test web pages and work with Playwright tests.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Running and Debugging Playwright tests** [references/playwright-tests.md](references/playwright-tests.md); Request mocking** [references/request-mocking.md](references/request-mocking.md); Running Playwright code** [references/running-code.md](references/running-code.md); Browser session management** [references/session-management.md](references/session-management.md); Spec-driven testing (plan / generate / heal)** [references/spec-driven-testing.md](references/spec-driven-testing.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Running and Debugging Playwright tests** [references/playwright-tests.md](references/playwright-tests.md); Spec-driven testing (plan / generate / heal)** [references/spec-driven-testing.md](references/spec-driven-testing.md); Test generation** [references/test-generation.md](references/test-generation.md)
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### pnpm
- Path: `C:\Users\jingr\codex-skills\pnpm\SKILL.md`
- Purpose: Node.js package manager with strict dependency resolution. Use when running pnpm specific commands, configuring workspaces, or managing dependencies with catalogs, patches, or overrides.
- Categories: A_LifeBoard_core, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Important:** When working with pnpm projects, agents should check for `pnpm-workspace.yaml` and `.npmrc` files to understand workspace structure and configuration. Always use `--frozen-lockfile` in CI environments.
- Must-follow rules: Important:** When working with pnpm projects, agents should check for `pnpm-workspace.yaml` and `.npmrc` files to understand workspace structure and configuration. Always use `--frozen-lockfile` in CI environments.
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### polish
- Path: `C:\Users\jingr\codex-skills\polish\SKILL.md`
- Purpose: polish
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### pseudo-elements
- Path: `C:\Users\jingr\codex-skills\pseudo-elements\SKILL.md`
- Purpose: Audit CSS for pseudo-element best practices and View Transitions API usage. Use when reviewing hover effects, decorative layers, or page transitions. Outputs file:line findings.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format; [MDN Pseudo-elements Reference](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/Pseudo-elements)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: May suggest new animation dependencies; LifeBoard currently has none of these dependencies.

### quieter
- Path: `C:\Users\jingr\codex-skills\quieter\SKILL.md`
- Purpose: quieter
- Categories: C_conditional
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### rams
- Path: `C:\Users\jingr\codex-skills\rams\SKILL.md`
- Purpose: Rams Design Review
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### react-doctor
- Path: `C:\Users\jingr\codex-skills\react-doctor\SKILL.md`
- Purpose: Use when finishing a feature, fixing a bug, before committing React code, or when the user types `/doctor`, asks to scan, triage, or clean up React diagnostics. Covers lint, accessibility, bundle size, architecture. Includes a regression check and a full local-triage workflow that fetches the canonical playbook.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### react-native-best-practices
- Path: `C:\Users\jingr\codex-skills\react-native-best-practices\SKILL.md`
- Purpose: Provides React Native performance optimization guidelines for FPS, TTI, bundle size, memory leaks, re-renders, and animations. Applies to tasks involving Hermes optimization, JS thread blocking, bridge overhead, FlashList, native modules, or debugging jank and frame drops.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Quick Reference**: Summary tables for conceptual skills; Treat shell commands in these references as local developer operations. Review them before running, prefer version-pinned tooling, and avoid piping remote scripts directly to a shell.; Measure**: Capture baseline metrics before changes. For runtime issues, prefer commit timeline, re-render counts, slow components, heaviest-commit breakdown, and startup/TTI when available. Component tree depth or count are optional context, not substitutes.; Optimize**: Apply the targeted fix from the relevant reference; Check library versions before suggesting API-specific fixes. Example: FlashList v2 deprecates `estimatedItemSize`, so do not flag it as missing there.
- Must-follow rules: Treat Re.Pack code splitting as first-party artifact delivery only. Remote chunks must come from trusted HTTPS origins you control and be pinned to the current app release.
- Output/acceptance signals: Treat third-party libraries and plugins as dependencies that still require normal supply-chain controls: pin versions, verify provenance, and update through your standard review process.; Verify improvement after optimization:**
- Forbidden/caution signals: Treat shell commands in these references as local developer operations. Review them before running, prefer version-pinned tooling, and avoid piping remote scripts directly to a shell.; Check library versions before suggesting API-specific fixes. Example: FlashList v2 deprecates `estimatedItemSize`, so do not flag it as missing there.; Do not suggest `useMemo` or `useCallback` dependency changes unless behavior is demonstrably incorrect or profiling shows wasted work tied to that value.; Do not report stale closures speculatively. Show the stale read path, a repro, or profiler evidence before calling it out.; When profiling a flow, measure the target interaction itself. Do not treat component tree depth or component count as the main performance evidence.
- Potential conflicts: No major conflict identified; use within task scope.

### react-router-framework-mode
- Path: `C:\Users\jingr\codex-skills\react-router-framework-mode\SKILL.md`
- Purpose: Build full-stack React applications using React Router's framework mode. Use when configuring routes, working with loaders and actions, handling forms, handling navigation, pending/optimistic UI, error boundaries, or working with react-router.config.ts or other react router conventions.
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Global UI belongs in `root.tsx`** - don't create separate layout files for nav/footer:; Use nested routes** for section-specific layouts. See `references/routing.md`.
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### redesign-existing-projects
- Path: `C:\Users\jingr\codex-skills\redesign-existing-projects\SKILL.md`
- Purpose: Upgrades existing websites and apps to premium quality. Audits current design, identifies generic AI patterns, and applies high-end design standards without breaking functionality. Works with any CSS framework or vanilla CSS.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Scan** ? Read the codebase. Identify the framework, styling method (Tailwind, vanilla CSS, styled-components, etc.), and current design patterns.; Body text too wide.** Limit paragraph width to roughly 65 characters. Increase line-height for readability.; Light/dark toggle always a sun/moon switch.** Use a dropdown, system preference detection, or integrate it into settings.; Missing alt text on images.** Describe image content for screen readers. Never leave `alt=""` or `alt="image"` on meaningful images.; Commented-out dead code.** Remove all debug artifacts before shipping.
- Must-follow rules: Symmetrical vertical padding.** Top and bottom padding are always identical. Adjust optically ? bottom padding often needs to be slightly larger.; Dashboard always has a left sidebar.** Try top navigation, a floating command menu, or a collapsible panel instead.; Mathematical alignment that looks optically wrong.** Centering by the math doesn't always look centered to the eye. Icons next to text, play buttons in circles, or text in buttons often need 1-2px optical adjustments to feel right.; Always one filled button + one ghost button.** Add text links or tertiary styles to reduce visual noise.; Light/dark toggle always a sun/moon switch.** Use a dropdown, system preference detection, or integrate it into settings.; Footer link farm with 4 columns.** Simplify. Focus on main navigational paths and legally required links.
- Output/acceptance signals: Random dark sections in a light mode page (or vice versa).** A single dark-background section breaking an otherwise light page looks like a copy-paste accident. Either commit to a full dark mode or keep a consistent background tone throughout. If contrast is needed, use a slightly darker shade of the same palette ? not a sudden jump to `#111` in the middle of a cream page.; Passive voice.** Use active voice: "We couldn't save your changes" instead of "Mistakes were made."; card carousel testimonials with dots.** Replace with a masonry wall, embedded social posts, or a single rotating quote.; Do not break existing functionality. Test after every change.
- Forbidden/caution signals: Fix** ? Apply targeted upgrades working with the existing stack. Do not rewrite from scratch. Improve what's there.; No error states.** Add clear, inline error messages for forms. Do not use `window.alert()`.; AI copywriting cliches.** Never use "Elevate", "Seamless", "Unleash", "Next-Gen", "Game-changer", "Delve", "Tapestry", or "In the world of...". Write plain, specific language.; Lorem Ipsum.** Never use placeholder latin text. Write real draft copy.; Missing alt text on images.** Describe image content for screen readers. Never leave `alt=""` or `alt="image"` on meaningful images.
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.

### refactor
- Path: `C:\Users\jingr\codex-skills\refactor\SKILL.md`
- Purpose: refactor
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Identify WCAG, semantic HTML, keyboard, focus, contrast, and form issues relevant to $name.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### remotion-best-practices
- Path: `C:\Users\jingr\codex-skills\remotion-best-practices\SKILL.md`
- Purpose: Best practices for Remotion - Video creation in React
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### shadcn
- Path: `C:\Users\jingr\codex-skills\shadcn\SKILL.md`
- Purpose: Manages shadcn components and projects ? adding, searching, fixing, debugging, styling, and composing UI. Provides project context, component docs, and usage examples. Applies when working with shadcn/ui, component registries, presets, --preset codes, or any project with a components.json file. Also triggers for "shadcn init", "create an app with --preset", or "switch to --preset".
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Use existing components first.** Use `npx shadcn@latest search` to check registries before writing custom UI. Check community registries too.; Use built-in variants before custom styles.** `variant="outline"`, `size="sm"`, etc.; Use existing components before custom markup.** Check if a component exists before writing a styled `div`.; `isRSC`** ? when `true`, components using `useState`, `useEffect`, event handlers, or browser APIs need `"use client"` at the top of the file. Always reference this field when advising on the directive.; `tailwindCssFile`** ? the global CSS file where custom CSS variables are defined. Always edit this file, never create a new one.
- Must-follow rules: Items always inside their Group.** `SelectItem` ? `SelectGroup`. `DropdownMenuItem` ? `DropdownMenuGroup`. `CommandItem` ? `CommandGroup`.; Dialog, Sheet, and Drawer always need a Title.** `DialogTitle`, `SheetTitle`, `DrawerTitle` required for accessibility. Use `className="sr-only"` if visually hidden.; `TabsTrigger` must be inside `TabsList`.** Never render triggers directly in `Tabs`.; `Avatar` always needs `AvatarFallback`.** For when the image fails to load.; `isRSC`** ? when `true`, components using `useState`, `useEffect`, event handlers, or browser APIs need `"use client"` at the top of the file. Always reference this field when advising on the directive.; `tailwindCssFile`** ? the global CSS file where custom CSS variables are defined. Always edit this file, never create a new one.
- Output/acceptance signals: Use existing components first.** Use `npx shadcn@latest search` to check registries before writing custom UI. Check community registries too.; Use `asChild` (radix) or `render` (base) for custom triggers.** Check `base` field from `npx shadcn@latest info`. ? [base-vs-radix.md](./rules/base-vs-radix.md); Callouts use `Alert`.** Don't build custom styled divs.; Empty states use `Empty`.** Don't build custom empty state markup.; Pass icons as objects, not string keys.** `icon={CheckIcon}`, not a string lookup.
- Forbidden/caution signals: `className` for layout, not styling.** Never override component colors or typography.; Forms use `FieldGroup` + `Field`.** Never use raw `div` with `space-y-*` or `grid gap-*` for form layout.; `InputGroup` uses `InputGroupInput`/`InputGroupTextarea`.** Never raw `Input`/`Textarea` inside `InputGroup`.; `TabsTrigger` must be inside `TabsList`.** Never render triggers directly in `Tabs`.; Never decode preset codes or build preset URLs manually.** Use `npx shadcn@latest preset decode <code>`, `preset url <code>`, or `preset open <code>`. For project-aware preset detection, use `npx shadcn@latest preset resolve`.
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### shape
- Path: `C:\Users\jingr\codex-skills\shape\SKILL.md`
- Purpose: shape
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### slidev
- Path: `C:\Users\jingr\codex-skills\slidev\SKILL.md`
- Purpose: Create and present web-based slidedecks for developers using Slidev with Markdown, Vue components, code highlighting, animations, and interactive features. Use when building technical presentations, conference talks, code walkthroughs, teaching materials, or developer decks.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: Export prerequisite**: `pnpm add -D playwright-chromium` is required for PDF/PPTX/PNG export. If export fails with a browser error, install this dependency first.
- Output/acceptance signals: Verify**: After `pnpm run dev`, confirm slides load at `http://localhost:3030`. After `pnpm run export`, check the output PDF exists in the project root.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### sounds-on-the-web
- Path: `C:\Users\jingr\codex-skills\sounds-on-the-web\SKILL.md`
- Purpose: Audit UI code for audio feedback best practices. Use when reviewing sound implementation, checking audio UX decisions, or auditing accessibility. Outputs file:line findings.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### stitch-design-taste
- Path: `C:\Users\jingr\codex-skills\stitch-design-taste\SKILL.md`
- Purpose: Semantic Design System Skill for Google Stitch. Generates agent-friendly DESIGN.md files that enforce premium, anti-generic UI standards ? strict typography, calibrated color, asymmetric layouts, perpetual micro-motion, and hardware-accelerated performance.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Start with the atmosphere ? understand the vibe before detailing tokens
- Must-follow rules: Serif Ban:** Generic serif fonts (`Times New Roman`, `Georgia`, `Garamond`, `Palatino`) are BANNED. If serif is needed for editorial/creative contexts, use only distinctive modern serifs: `Fraunces`, `Gambarino`, `Editorial New`, or `Instrument Serif`. Serif is always BANNED in dashboards or software UIs; High-Density Override:** When density exceeds 7, all numbers must use Monospace; No Overlapping:** Text must never overlap images or other text. Every element occupies its own clean spatial zone; Full-height sections must use `min-h-[100dvh]` ? never `h-screen` (iOS Safari catastrophic jump); No overlapping elements ? clean spatial separation always
- Output/acceptance signals: Use absolute neutral bases (Zinc/Slate) with high-contrast singular accents
- Forbidden/caution signals: Never use pure black (`#000000`) ? use Off-Black, Zinc-950, or Charcoal; No Overlapping:** Text must never overlap images or other text. Every element occupies its own clean spatial zone; CSS Grid over Flexbox math ? never use `calc()` percentage hacks; Full-height sections must use `min-h-[100dvh]` ? never `h-screen` (iOS Safari catastrophic jump); Staggered Orchestration:** Never mount lists instantly ? use cascade delays for waterfall reveals
- Potential conflicts: No major conflict identified; use within task scope.

### svelte-code-writer
- Path: `C:\Users\jingr\codex-skills\svelte-code-writer\SKILL.md`
- Purpose: CLI tools for Svelte 5 documentation lookup and code analysis. MUST be used whenever creating, editing or analyzing any Svelte component (.svelte) or Svelte module (.svelte.ts/.svelte.js). If possible, this skill should be executed within the svelte-file-editor agent for optimal results.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Always validate** - Run `svelte-autofixer` before finalizing any Svelte component
- Must-follow rules: Always validate** - Run `svelte-autofixer` before finalizing any Svelte component
- Output/acceptance signals: Important:** When passing code with runes (`$state`, `$derived`, etc.) via the terminal, escape the `$` character as `\$` to prevent shell variable substitution.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### swiftui-ui-patterns
- Path: `C:\Users\jingr\codex-skills\swiftui-ui-patterns\SKILL.md`
- Purpose: Best practices and example-driven guidance for building SwiftUI views and components, including navigation hierarchies, custom view modifiers, and responsive layouts with stacks and grids. Use when creating or refactoring SwiftUI UI, designing tab architecture with TabView, composing screens with VStack/HStack, managing @State or @Binding, building declarative iOS interfaces, or needing component-specific patterns and examples.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Find a nearby example in the repo with `rg "TabView\("` or similar, then read the closest SwiftUI view.; Choose the relevant component reference from `references/components-index.md` and follow its guidance.; If the interaction reveals secondary content by dragging or scrolling the primary content away, read `references/scroll-reveal.md` before implementing gestures manually.; Start with `references/app-wiring.md` to wire TabView + NavigationStack + sheets.; Choose the next component reference based on the UI you need first (TabView, NavigationStack, Sheets).
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Build the view with small, focused subviews and SwiftUI-native data flow.; Sketch the view hierarchy, routing model, and presentation points; extract repeated parts into subviews. For complex navigation, read `references/navigationstack.md`, `references/sheets.md`, or `references/deeplinks.md`. **Build and verify no compiler errors before proceeding.**
- Forbidden/caution signals: Use modern SwiftUI state (`@State`, `@Binding`, `@Observable`, `@Environment`) and avoid unnecessary view models.; Prefer the newest SwiftUI API that fits the deployment target and call out the minimum OS whenever a pattern depends on it.; Sheets**: Prefer `.sheet(item:)` over `.sheet(isPresented:)` when state represents a selected model. Avoid `if let` inside a sheet body. Sheets should own their actions and call `dismiss()` internally instead of forwarding `onCancel`/`onConfirm` closures.; Scroll-driven reveals**: Prefer deriving a normalized progress value from scroll offset and driving the visual state from that single source of truth. Avoid parallel gesture state machines unless scroll alone cannot express the interaction.
- Potential conflicts: No major conflict identified; use within task scope.

### swiss-design
- Path: `C:\Users\jingr\codex-skills\swiss-design\SKILL.md`
- Purpose: Apply a Swiss International Style design system using Tailwind CSS. Use when asked to style a webpage, clean up a UI, apply a design system, make something look great, or when the user references Swiss design, grotesque fonts, Helvetica, grid systems, modernist typography, or 1960s/1950s design aesthetics. Implements IBM Plex Sans typography, stone color palette, opacity-based hierarchy, generous whitespace, and structured grid layouts.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Fallback chain** (in order of preference):
- Must-follow rules: Mobile first, always.** Design for the smallest viewport first, then expand. Every layout must work at 320px and at 1440px. Use `sm:`, `md:`, `lg:` Tailwind prefixes systematically.; Touch targets:** All interactive elements must be at least 44?44px on mobile. Use `min-h-[44px] min-w-[44px]` for buttons and nav links.; IBM Plex Sans is not a system font.** Always include the Google Fonts `<link>` tag.; Every layout must work on mobile.** Default (no prefix) classes are mobile. Always add `md:` and `lg:` variants for larger viewports. Never build desktop-first and try to retrofit mobile.; Touch targets minimum 44px.** Buttons, links, and nav items must be tappable on mobile.
- Output/acceptance signals: Every layout must work on mobile.** Default (no prefix) classes are mobile. Always add `md:` and `lg:` variants for larger viewports. Never build desktop-first and try to retrofit mobile.
- Forbidden/caution signals: Headings: `font-light` (300) or `font-normal` (400). Never bold for headings.; Emphasis within body: `font-medium` (500). Never `font-bold` (700).; Use the ellipsis character `?`, never three periods `...`.; Use curly quotes `"` `"` and `'` `'`, never straight quotes `"` `'`.; Navigation on mobile:** Collapse to a minimal top bar. Hide secondary nav links below `md:`. Never use hamburger menus with deeply nested hierarchies ? the Swiss style favors flat, clear navigation.
- Potential conflicts: No major conflict identified; use within task scope.

### threejs-animation
- Path: `C:\Users\jingr\codex-skills\threejs-animation\SKILL.md`
- Purpose: Three.js animation - keyframe animation, skeletal animation, morph targets, animation mixing. Use when animating objects, playing GLTF animations, creating procedural motion, or blending animations.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-fundamentals
- Path: `C:\Users\jingr\codex-skills\threejs-fundamentals\SKILL.md`
- Purpose: Three.js scene setup, cameras, renderer, Object3D hierarchy, coordinate systems. Use when setting up 3D scenes, creating cameras, configuring renderers, managing object hierarchies, or working with transforms.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-geometry
- Path: `C:\Users\jingr\codex-skills\threejs-geometry\SKILL.md`
- Purpose: Three.js geometry creation - built-in shapes, BufferGeometry, custom geometry, instancing. Use when creating 3D shapes, working with vertices, building custom meshes, or optimizing with instanced rendering.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-interaction
- Path: `C:\Users\jingr\codex-skills\threejs-interaction\SKILL.md`
- Purpose: Three.js interaction - raycasting, controls, mouse/touch input, object selection. Use when handling user input, implementing click detection, adding camera controls, or creating interactive 3D experiences.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-lighting
- Path: `C:\Users\jingr\codex-skills\threejs-lighting\SKILL.md`
- Purpose: Three.js lighting - light types, shadows, environment lighting. Use when adding lights, configuring shadows, setting up IBL, or optimizing lighting performance.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-loaders
- Path: `C:\Users\jingr\codex-skills\threejs-loaders\SKILL.md`
- Purpose: Three.js asset loading - GLTF, textures, images, models, async patterns. Use when loading 3D models, textures, HDR environments, or managing loading progress.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-materials
- Path: `C:\Users\jingr\codex-skills\threejs-materials\SKILL.md`
- Purpose: Three.js materials - PBR, basic, phong, shader materials, material properties. Use when styling meshes, working with textures, creating custom shaders, or optimizing material performance.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Use alphaTest instead of transparency**: When applicable, faster
- Forbidden/caution signals: Avoid transparent when possible**: Transparent materials require sorting
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-postprocessing
- Path: `C:\Users\jingr\codex-skills\threejs-postprocessing\SKILL.md`
- Purpose: Three.js post-processing - EffectComposer, bloom, DOF, screen effects. Use when adding visual effects, color grading, blur, glow, or creating custom screen-space shaders.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Limit passes**: Each pass adds a full-screen render; Lower resolution**: Use smaller render targets for blur passes; Disable unused effects**: Toggle passes on/off
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-shaders
- Path: `C:\Users\jingr\codex-skills\threejs-shaders\SKILL.md`
- Purpose: Three.js shaders - GLSL, ShaderMaterial, uniforms, custom effects. Use when creating custom visual effects, modifying vertices, writing fragment shaders, or extending built-in materials.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Avoid conditionals**: Use mix/step instead of if/else
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### threejs-textures
- Path: `C:\Users\jingr\codex-skills\threejs-textures\SKILL.md`
- Purpose: Three.js textures - texture types, UV mapping, environment maps, texture settings. Use when working with images, UV coordinates, cubemaps, HDR environments, or texture optimization.
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: `threejs-loaders` - Loading texture files
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: Three.js/WebGL conflicts with current LifeBoard weather atmosphere constraints unless explicitly approved.

### to-spring-or-not-to-spring
- Path: `C:\Users\jingr\codex-skills\to-spring-or-not-to-spring\SKILL.md`
- Purpose: Audit animation code for correct timing function selection. Use when reviewing motion implementations, debugging animations that feel wrong, or choosing between springs and easing. Outputs file:line findings.
- Categories: B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Output findings in `file:line` format
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Pass:**
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### transitions-dev
- Path: `C:\Users\jingr\codex-skills\transitions-dev\SKILL.md`
- Purpose: Production-ready CSS transitions for web apps. Use when implementing notification badges, dropdowns, modals, panel reveals, page transitions, card resizes, number pop-ins, text swaps, icon swaps, success checks, avatar group hovers, or error state shakes. Triggers on "add a transition", "animate the dropdown", "make the modal open smoothly", "swap icon", "page slide", "stagger animation", "open / close transition", "make it animate", "tween the size", "fade between", "smooth open", "smooth close", "success animation", "checkmark animation", "confirmation animation", "form error", "shake on invalid", "validation feedback", "hover lift", "avatar stack hover", "chip group hover".
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Confirmation / success / "done" moment** (checkmark, payment processed, file uploaded) ? success check.; Add the `:root` block above** to the user's global stylesheet, but only if it isn't already there. If the user already imported the universal install block once, do **not** duplicate it.; Paste the chosen transition's CSS verbatim** from the relevant reference file. Do not rewrite selectors, do not collapse the transition into shorthand, do not strip `will-change`. The snippets are tuned and tested.; Setting `transition-timing-function` in CSS** for the avatar group hover ? it has to be set inline in JS *before* the `--shift` / `--scale-active` writes so the bouncy ease-out only applies on `mouseleave`.; [_root.css](./_root.css) ? the universal install block on its own, ready to import directly.
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Paste the chosen transition's CSS verbatim** from the relevant reference file. Do not rewrite selectors, do not collapse the transition into shorthand, do not strip `will-change`. The snippets are tuned and tested.
- Forbidden/caution signals: Paste the chosen transition's CSS verbatim** from the relevant reference file. Do not rewrite selectors, do not collapse the transition into shorthand, do not strip `will-change`. The snippets are tuned and tested.
- Potential conflicts: No major conflict identified; use within task scope.

### tsdown
- Path: `C:\Users\jingr\codex-skills\tsdown\SKILL.md`
- Purpose: Bundle TypeScript and JavaScript libraries with blazing-fast speed powered by Rolldown. Use when building libraries, generating type declarations, bundling for multiple formats, or migrating from tsup.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Generating TypeScript declaration files (.d.ts)
- Must-follow rules: Always generate type declarations** for TypeScript libraries:
- Output/acceptance signals: Building TypeScript/JavaScript libraries for npm; Building React, Vue, Solid, or Svelte component libraries
- Forbidden/caution signals: Externalize dependencies** to avoid bundling unnecessary code:
- Potential conflicts: No major conflict identified; use within task scope.

### turborepo
- Path: `C:\Users\jingr\codex-skills\turborepo\SKILL.md`
- Purpose: |
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Before flagging missing `outputs`, check what the task actually produces:**; Read the package's script (e.g., `"build": "tsc"`, `"test": "vitest"`); Determine if it writes files to disk or only outputs to stdout; Only flag if the task produces files that should be cached; TypeScript `--noEmit` can still produce cache files:**
- Must-follow rules: DO NOT create Root Tasks. ALWAYS create package tasks.**; Always use `turbo run` when the command is written into code:**; If you must share variables**, use `globalEnv` to be explicit about what's shared, and document why.
- Output/acceptance signals: run: turbo run build --affected; The shorthand `turbo <tasks>` is ONLY for one-off terminal commands** typed directly by humans or agents. Never write `turbo build` into package.json, CI, or scripts.; run: turbo build --affected; If dependencies ARE declared** (e.g., `"@repo/types": "workspace:*"` in package.json), remove the `prebuild` script. Turbo's `dependsOn: ["^build"]` handles this automatically.; If dependencies are NOT declared**, the `prebuild` exists because `^build` won't trigger without a dependency relationship. The fix is to:
- Forbidden/caution signals: DO NOT create Root Tasks. ALWAYS create package tasks.**; DO NOT** put task logic in root `package.json`. This defeats Turborepo's parallelization.; The shorthand `turbo <tasks>` is ONLY for one-off terminal commands** typed directly by humans or agents. Never write `turbo build` into package.json, CI, or scripts.
- Potential conflicts: No major conflict identified; use within task scope.

### typeset
- Path: `C:\Users\jingr\codex-skills\typeset\SKILL.md`
- Purpose: typeset
- Categories: D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### ui-ux-pro-max
- Path: `C:\Users\jingr\codex-skills\ui-ux-pro-max\SKILL.md`
- Purpose: UI/UX design intelligence for web and mobile. Includes 50+ styles, 161 color palettes, 57 font pairings, 161 product types, 99 UX guidelines, and 25 chart types across 10 stacks (React, Next.js, Vue, Svelte, SwiftUI, React Native, Flutter, Tailwind, shadcn/ui, and HTML/CSS). Actions: plan, build, create, design, implement, review, fix, improve, optimize, enhance, refactor, and check UI/UX code. Projects: website, landing page, dashboard, admin panel, e-commerce, SaaS, portfolio, blog, and mobile app. Elements: button, modal, navbar, sidebar, card, table, form, and chart. Styles: glassmorphism, claymorphism, minimalism, brutalism, neumorphism, bento grid, dark mode, responsive, skeuomorphism, and flat design. Topics: color systems, accessibility, animation, layout, typography, font pairing, spacing, interaction states, shadow, and gradient. Integrations: shadcn/ui MCP for component search and examples.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: For human/AI reference: follow priority 1?10 to decide which rule category to focus on first; use `--domain <Domain>` to query details when needed. Scripts do not read this table.*; `voiceover-sr` - Meaningful accessibilityLabel/accessibilityHint; logical reading order for VoiceOver/screen readers (Apple HIG, MD); `drag-threshold` - Use a movement threshold before starting drag to avoid accidental drags; `reduce-reflows` - Avoid frequent layout reads/writes; batch DOM reads then writes; `main-thread-budget` - Keep per-frame work under ~16ms for 60fps; move heavy tasks off main thread (HIG, MD)
- Must-follow rules: `gesture-alternative` - Don't rely on gesture-only interactions; always provide visible controls for critical actions; `no-precision-required` - Avoid requiring pixel-perfect taps on small icons or thin edges; `swipe-clarity` - Swipe actions must show clear affordance or hint (chevron, label, tutorial); `fixed-element-offset` - Fixed navbar/bottom bar must reserve safe padding for underlying content; `color-accessible-pairs` - Foreground/background pairs must meet 4.5:1 (AA) or 7:1 (AAA); use tools to verify (WCAG, MD); `color-not-decorative-only` - Functional color (error red, success green) must include icon/text; avoid color-only meaning (HIG, MD)
- Output/acceptance signals: Building design systems or reusable component libraries; `color-contrast` - Minimum 4.5:1 ratio for normal text (large text 3:1); Material Design; `dark-mode-pairing` - Design light/dark variants together to keep brand, contrast, and style consistent; `visual-hierarchy` - Establish hierarchy via size, spacing, contrast ? not color alone; `contrast-readability` - Darker text on light backgrounds (e.g. slate-900 on white)
- Forbidden/caution signals: For human/AI reference: follow priority 1?10 to decide which rule category to focus on first; use `--domain <Domain>` to query details when needed. Scripts do not read this table.*; `dynamic-type` - Support system text scaling; avoid truncation as text grows (Apple Dynamic Type, MD); `gesture-conflicts` - Avoid horizontal swipe on main content; prefer vertical scroll; `haptic-feedback` - Use haptic for confirmations and important actions; avoid overuse (Apple HIG); `no-precision-required` - Avoid requiring pixel-perfect taps on small icons or thin edges
- Potential conflicts: Multiple broad design skills can compete for overall design direction; choose one primary design skill per stage.; Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### unocss
- Path: `C:\Users\jingr\codex-skills\unocss\SKILL.md`
- Purpose: UnoCSS instant atomic CSS engine, superset of Tailwind CSS. Use when configuring UnoCSS, writing utility rules, shortcuts, or working with presets like Wind, Icons, Attributify.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Important:** Before writing UnoCSS code, agents should check for `uno.config.*` or `unocss.config.*` files in the project root to understand what presets, rules, and shortcuts are available. If the project setup is unclear, avoid using attributify mode and other advanced features - stick to basic `class` usage.
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Important:** Before writing UnoCSS code, agents should check for `uno.config.*` or `unocss.config.*` files in the project root to understand what presets, rules, and shortcuts are available. If the project setup is unclear, avoid using attributify mode and other advanced features - stick to basic `class` usage.
- Potential conflicts: No major conflict identified; use within task scope.

### use-of-color
- Path: `C:\Users\jingr\codex-skills\use-of-color\SKILL.md`
- Purpose: use-of-color
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Identify WCAG, semantic HTML, keyboard, focus, contrast, and form issues relevant to $name.
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vercel-react-best-practices
- Path: `C:\Users\jingr\codex-skills\vercel-react-best-practices\SKILL.md`
- Purpose: React and Next.js performance optimization guidelines from Vercel Engineering. This skill should be used when writing, reviewing, or refactoring React/Next.js code to ensure optimal performance patterns. Triggers on tasks involving React components, Next.js pages, data fetching, bundle optimization, or performance improvements.
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: `async-cheap-condition-before-await` - Check cheap sync conditions before awaiting flags or remote values; `bundle-barrel-imports` - Import directly, avoid barrel files; `bundle-analyzable-paths` - Prefer statically analyzable import and file-system paths to avoid broad bundles and traces; `rerender-defer-reads` - Don't subscribe to state only used in callbacks; `js-cache-storage` - Cache localStorage/sessionStorage reads
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: `server-serialization` - Minimize data passed to client components; `client-passive-event-listeners` - Use passive listeners for scroll; `rerender-lazy-state-init` - Pass function to useState for expensive values; `js-index-maps` - Build Map for repeated lookups; `js-flatmap-filter` - Use flatMap to map and filter in one pass
- Forbidden/caution signals: `bundle-barrel-imports` - Import directly, avoid barrel files; `bundle-analyzable-paths` - Prefer statically analyzable import and file-system paths to avoid broad bundles and traces; `server-dedup-props` - Avoid duplicate serialization in RSC props; `server-no-shared-module-state` - Avoid module-level mutable request state in RSC/SSR; `rerender-simple-expression-in-memo` - Avoid memo for simple primitives
- Potential conflicts: No major conflict identified; use within task scope.

### vite
- Path: `C:\Users\jingr\codex-skills\vite\SKILL.md`
- Purpose: Vite build tool configuration, plugin API, SSR, and Vite 8 Rolldown migration. Use when working with Vite projects, vite.config.ts, Vite plugins, or building libraries/SSR apps with Vite.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: Always use ESM, avoid CommonJS
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Always use ESM, avoid CommonJS
- Potential conflicts: No major conflict identified; use within task scope.

### vitepress
- Path: `C:\Users\jingr\codex-skills\vitepress\SKILL.md`
- Purpose: VitePress static site generator powered by Vite and Vue. Use when building documentation sites, configuring themes, or writing Markdown with Vue components.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: File-based routing with `.md` files; Before working with VitePress projects:**
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vitest
- Path: `C:\Users\jingr\codex-skills\vitest\SKILL.md`
- Purpose: Vitest fast unit testing framework powered by Vite with Jest-compatible API. Use when writing tests, mocking, configuring coverage, or working with test filtering and fixtures.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Multi-threaded workers for parallel test execution
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Vite-native: Uses Vite's transformation pipeline for fast HMR-like test updates; Jest-compatible: Drop-in replacement for most Jest test suites; Smart watch mode: Only reruns affected tests based on module graph; Multi-threaded workers for parallel test execution; Snapshot testing, mocking, and spy utilities
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-best-practices\SKILL.md`
- Purpose: MUST be used for Vue.js tasks. Strongly recommends Composition API with `<script setup>` and TypeScript as the standard approach. Covers Vue 3, SSR, Volar, vue-tsc. Load for any Vue, .vue files, Vue Router, Pinia, or Vite with Vue work. ALWAYS use Composition API unless the project explicitly requires Options API.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Readability counts:** write clear, self-documenting code.; Before implementing any Vue task, make sure to read and apply these core references:; `references/reactivity.md`; `references/sfc.md`; `references/component-data-flow.md`
- Must-follow rules: Must-read reference from `1.1`: [reactivity](references/reactivity.md); Must-read reference from `1.1`: [sfc](references/sfc.md); Must-read reference from `1.1`: [component-data-flow](references/component-data-flow.md); Must-read reference from `1.1`: [composables](references/composables.md); Fallthrough attributes: wrapper/base components must forward attrs/events safely -> [component-fallthrough-attrs](references/component-fallthrough-attrs.md); Animation-related features: pick the simplest approach that matches the required motion behavior.
- Output/acceptance signals: Favor small, focused components:** easier to test, reuse, and maintain.
- Forbidden/caution signals: Avoid unnecessary re-renders:** use computed properties and watchers wisely.; Avoid recomputing expensive logic in templates.; Do not place full feature implementations in entry/root/view components when those features contain independent parts.; Over-abstraction in hot list paths -> [perf-avoid-component-abstraction-in-lists](references/perf-avoid-component-abstraction-in-lists.md)
- Potential conflicts: No major conflict identified; use within task scope.

### vue-debug-guides
- Path: `C:\Users\jingr\codex-skills\vue-debug-guides\SKILL.md`
- Purpose: Vue 3 debugging and error handling for runtime errors, warnings, async failures, and SSR/hydration issues. Use when diagnosing or fixing Vue issues.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Tracing unexpected re-renders and state updates ? See [reactivity-debugging-hooks](reference/reactivity-debugging-hooks.md); Ref values not updating due to missing .value access ? See [ref-value-access](reference/ref-value-access.md); State stops updating after destructuring reactive objects ? See [reactive-destructuring](reference/reactive-destructuring.md); Refs inside arrays, Maps, or Sets not unwrapping ? See [refs-in-collections-need-value](reference/refs-in-collections-need-value.md); Nested refs rendering as [object Object] in templates ? See [template-ref-unwrapping-top-level](reference/template-ref-unwrapping-top-level.md)
- Must-follow rules: Reactive proxy identity comparisons always return false ? See [reactivity-proxy-identity-hazard](reference/reactivity-proxy-identity-hazard.md); defineEmits used inside function or conditional ? See [defineEmits-must-be-top-level](reference/defineEmits-must-be-top-level.md); v-else elements rendering unconditionally always ? See [v-else-must-follow-v-if](reference/v-else-must-follow-v-if.md); Reused vnode instances render incorrectly ? See [render-function-vnodes-must-be-unique](reference/render-function-vnodes-must-be-unique.md); Teleport target element not found in DOM ? See [teleport-target-must-exist](reference/teleport-target-must-exist.md)
- Output/acceptance signals: Passing parameters to computed properties fails ? See [computed-no-parameters](reference/computed-no-parameters.md); Combining passive and prevent modifiers breaks event behavior ? See [no-passive-with-prevent](reference/no-passive-with-prevent.md); List children re-render unnecessarily because parent passes unstable props ? See [perf-props-stability-update-optimization](reference/perf-props-stability-update-optimization.md)
- Forbidden/caution signals: Computed value never updates after conditional logic ? See [computed-conditional-dependencies](reference/computed-conditional-dependencies.md); Watcher never triggers for reactive object properties ? See [watch-reactive-property-getter](reference/watch-reactive-property-getter.md); Lifecycle hooks registered asynchronously never run ? See [lifecycle-hooks-synchronous-registration](reference/lifecycle-hooks-synchronous-registration.md); Composable mutates external state unexpectedly ? See [composable-avoid-hidden-side-effects](reference/composable-avoid-hidden-side-effects.md); Accessing vnode internals breaks on Vue updates ? See [render-function-avoid-internal-vnode-properties](reference/render-function-avoid-internal-vnode-properties.md)
- Potential conflicts: Tailwind ideas may transfer, but React primitive/component requirements do not apply to Vue.

### vue-jsx-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-jsx-best-practices\SKILL.md`
- Purpose: JSX syntax in Vue (e.g., class vs className, JSX plugin config).
- Categories: C_conditional, D_currently_unrelated
- Suitable for LifeBoard: False
- Suitable for weather visual: False
- File-reading/input requirements: Migrating React JSX code to Vue or getting attribute type errors ? See [render-function-jsx-vue-vs-react](reference/render-function-jsx-vue-vs-react.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue-options-api-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-options-api-best-practices\SKILL.md`
- Purpose: Vue 3 Options API style (data(), methods, this context). Each reference shows Options API solution only.
- Categories: A_LifeBoard_core, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Need to enable TypeScript type inference for component properties ? See [ts-options-api-use-definecomponent](reference/ts-options-api-use-definecomponent.md); Enabling type safety for Options API this context ? See [ts-strict-mode-options-api](reference/ts-strict-mode-options-api.md); Using old TypeScript versions with prop validators ? See [ts-options-api-arrow-functions-validators](reference/ts-options-api-arrow-functions-validators.md); Event handler parameters need proper type safety ? See [ts-options-api-type-event-handlers](reference/ts-options-api-type-event-handlers.md); Need to type object or array props with interfaces ? See [ts-options-api-proptype-complex-types](reference/ts-options-api-proptype-complex-types.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue-pinia-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-pinia-best-practices\SKILL.md`
- Purpose: Pinia stores, state management patterns, store setup, and reactivity with stores.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Getting "getActivePinia was called" error at startup ? See [pinia-no-active-pinia-error](reference/pinia-no-active-pinia-error.md); Setup stores missing state in DevTools or SSR ? See [pinia-setup-store-return-all-state](reference/pinia-setup-store-return-all-state.md); Store destructuring stops updating UI reactively ? See [pinia-store-destructuring-breaks-reactivity](reference/pinia-store-destructuring-breaks-reactivity.md); Store methods lose context in template calls ? See [store-method-binding-parentheses](reference/store-method-binding-parentheses.md); Filters reset on refresh or can't be shared ? See [state-url-for-ephemeral-filters](reference/state-url-for-ephemeral-filters.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Building production app without DevTools or conventions ? See [state-use-pinia-for-large-apps](reference/state-use-pinia-for-large-apps.md)
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue-router-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-router-best-practices\SKILL.md`
- Purpose: Vue Router 4 patterns, navigation guards, route params, and route-component lifecycle interactions.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Navigating between same route with different params ? See [router-beforeenter-no-param-trigger](reference/router-beforeenter-no-param-trigger.md); Accessing component instance in beforeRouteEnter guard ? See [router-beforerouteenter-no-this](reference/router-beforerouteenter-no-this.md); Navigation guard making API calls without awaiting ? See [router-guard-async-await-pattern](reference/router-guard-async-await-pattern.md); Users trapped in infinite redirect loops ? See [router-navigation-guard-infinite-loop](reference/router-navigation-guard-infinite-loop.md); Navigation guard using deprecated next() function ? See [router-navigation-guard-next-deprecated](reference/router-navigation-guard-next-deprecated.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Building production single-page application ? See [router-use-vue-router-for-production](reference/router-use-vue-router-for-production.md)
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue-testing-best-practices
- Path: `C:\Users\jingr\codex-skills\vue-testing-best-practices\SKILL.md`
- Purpose: Use for Vue.js testing. Covers Vitest, Vue Test Utils, component testing, mocking, testing patterns, and Playwright for E2E testing.
- Categories: A_LifeBoard_core, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: Setting up test infrastructure for Vue 3 projects ? See [testing-vitest-recommended-for-vue](reference/testing-vitest-recommended-for-vue.md); Tests keep breaking when refactoring component internals ? See [testing-component-blackbox-approach](reference/testing-component-blackbox-approach.md); Tests fail intermittently with race conditions ? See [testing-async-await-flushpromises](reference/testing-async-await-flushpromises.md); Composables using lifecycle hooks or inject fail to test ? See [testing-composables-helper-wrapper](reference/testing-composables-helper-wrapper.md); Getting "injection Symbol(pinia) not found" errors in tests ? See [testing-pinia-store-setup](reference/testing-pinia-store-setup.md)
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Setting up test infrastructure for Vue 3 projects ? See [testing-vitest-recommended-for-vue](reference/testing-vitest-recommended-for-vue.md); Tests keep breaking when refactoring component internals ? See [testing-component-blackbox-approach](reference/testing-component-blackbox-approach.md); Tests fail intermittently with race conditions ? See [testing-async-await-flushpromises](reference/testing-async-await-flushpromises.md); Composables using lifecycle hooks or inject fail to test ? See [testing-composables-helper-wrapper](reference/testing-composables-helper-wrapper.md); Getting "injection Symbol(pinia) not found" errors in tests ? See [testing-pinia-store-setup](reference/testing-pinia-store-setup.md)
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vue
- Path: `C:\Users\jingr\codex-skills\vue\SKILL.md`
- Purpose: Vue 3 Composition API, script setup macros, reactivity system, and built-in components. Use when writing Vue SFCs, defineProps/defineEmits/defineModel, watchers, or using Transition/Teleport/Suspense/KeepAlive.
- Categories: A_LifeBoard_core, C_conditional, D_currently_unrelated
- Suitable for LifeBoard: True
- Suitable for weather visual: False
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: Always use Composition API over Options API
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### vueuse-functions
- Path: `C:\Users\jingr\codex-skills\vueuse-functions\SKILL.md`
- Purpose: Apply VueUse composables where appropriate to build concise, maintainable Vue.js / Nuxt features.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Prefer VueUse composables over custom code to improve readability, maintainability, and performance.; `EXTERNAL`: Use only if the user already installed the required external dependency; otherwise reconsider, and ask to install only if truly needed.
- Must-follow rules: Always check first whether a VueUse function can implement the requirement.; `EXTERNAL`: Use only if the user already installed the required external dependency; otherwise reconsider, and ask to install only if truly needed.
- Output/acceptance signals: not explicitly extracted
- Forbidden/caution signals: Apply this skill whenever assisting user development work in Vue.js / Nuxt.
- Potential conflicts: No major conflict identified; use within task scope.

### wcag-audit-patterns
- Path: `C:\Users\jingr\codex-skills\wcag-audit-patterns\SKILL.md`
- Purpose: Conduct WCAG 2.2 accessibility audits with automated testing, manual verification, and remediation guidance. Use when auditing websites for accessibility, fixing WCAG violations, or implementing accessible design patterns.
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: not explicitly extracted
- Must-follow rules: Don't rely only on automated testing** - Manual testing required
- Output/acceptance signals: Test with real users** - Disabled users provide best feedback; Document patterns** - Build accessible component library; Don't rely only on automated testing** - Manual testing required
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### web-design-guidelines
- Path: `C:\Users\jingr\codex-skills\web-design-guidelines\SKILL.md`
- Purpose: Review UI code for Web Interface Guidelines compliance. Use when asked to "review my UI", "check accessibility", "audit design", "review UX", or "check my site against best practices".
- Categories: B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Read the specified files (or prompt user for files/pattern); Read the specified files
- Must-follow rules: not explicitly extracted
- Output/acceptance signals: Fetch the latest guidelines from the source URL below
- Forbidden/caution signals: not explicitly extracted
- Potential conflicts: No major conflict identified; use within task scope.

### web-quality-audit
- Path: `C:\Users\jingr\codex-skills\web-quality-audit\SKILL.md`
- Purpose: Comprehensive web quality audit covering performance, accessibility, SEO, and best practices. Use when asked to "audit my site", "review web quality", "run lighthouse audit", "check page quality", or "optimize my website".
- Categories: A_LifeBoard_core, B_weather_visual, C_conditional
- Suitable for LifeBoard: True
- Suitable for weather visual: True
- File-reading/input requirements: Cache effectively.** Long cache TTLs for static assets. Immutable caching for hashed files.; Descriptive link text.** Not "click here" or "read more".; [Category]** Issue description. File: `path/to/file.js:123`; [ ] Test with screen reader; [Performance Optimization](../performance/SKILL.md)
- Must-follow rules: Core Web Vitals** ? Must pass for good page experience:; LCP (Largest Contentful Paint) < 2.5s.** The largest visible element must render quickly. Optimize images, fonts, and server response time.; INP (Interaction to Next Paint) < 200ms.** User interactions must feel instant. Reduce JavaScript execution time and break up long tasks.; CLS (Cumulative Layout Shift) < 0.1.** Content must not jump around. Set explicit dimensions on images, embeds, and ads.; HTTPS.** Secure connection required.
- Output/acceptance signals: Core Web Vitals** ? Must pass for good page experience:; Color contrast.** Minimum 4.5:1 for normal text, 3:1 for large text (WCAG AA).; No exposed source maps.** In production builds.; [ ] Core Web Vitals passing; [ ] Test with screen reader
- Forbidden/caution signals: Optimize CSS.** Extract critical CSS. Remove unused styles. Avoid `@import`.
- Potential conflicts: No major conflict identified; use within task scope.
